# Sprint Status
Last Updated: 2026-04-25 (Epic 23 DONE — 23.1 pushed by Ankit; Epic 24 Story 24.1 APPROVED by Murat cycle 1, moved to done — P0 hotfix forward-fix verified at unit + integration + observability + LIVE happy-path smoke against real Gemini API, SDK 1.73.1, PTB 22.7, 738/738 backend tests pass post-Murat parallel-test addition, AC9 clean-env satisfied, e2e thought_signature test added + gated behind RUN_LIVE_E2E=1, awaiting Ankit's commit + push + push-time deeper smoke; Epic 25 — Story 25.1 drafted (awaiting Parminder ready-approval pending Ankit's streaming-approach decision); **Bug 25.2 APPROVED by Murat cycle 1 of 3, moved to DONE 2026-04-25 — Harpreet review approved + Murat testing pass: 746/746 backend tests independently reproduced, 8 Bug 25.2 tests green, AC5 37-trace re-audit on existing Langfuse data reproduced Parminder's 5.539x overstatement on `1cd04de7` exactly + identified 15 cache-hit traces with per-trace overstatement 1.675x-6.550x and 1.515x aggregate (33.98% projected post-fix reduction), AC4 ±2% recon runbook documented for Ankit's post-push gate, AC7 +30d watchdog threshold recalibrated from ≥40% to ≥50% based on empirical 24h projection (theoretical ceiling ~63% with cache adoption, leaves headroom), AC2 Harpreet nit on chatgpt_provider:664 missing mirror comment confirmed but deferred to follow-up cleanup (AC2 grep-satisfied with 1 match), production clean (zero ERROR/CRITICAL past 1h, zero `cached_token_count_anomaly` matches), zero live API calls; ready for Ankit's push (likely bundling 24.1 + Telegram hotfix + Murat's testing additions + Story 25.1 docs + Bug 25.2)**)

## Project
- Name: Annie (Personal AI Companion)
- Tech Stack: Python 3.12+ / FastAPI / Redis / Docker Compose / React+Vite (web) / Grok-4+ChatGPT-5+Gemini
- Test Command: make test
- Build Command: make start

## Implementation Status Overview

### Completed Epics
- Epic 1: Foundation & Infrastructure (Stories 1.1-1.6) - DONE
- Epic 2: Core Chat & LLM Integration (Stories 2.1-2.7) - DONE
- Epic 3: Memory & Persistence (Stories 3.1-3.2) - DONE
- Epic 4: Decision Support Tools - Story 4.1 (Grok Live Search) - DONE
- Epic 5: Telegram Bot Interface (Stories 5.1-5.3) - DONE
- Epic 7: User Profile & Personalization (Stories 7.1-7.2) - DONE
- Epic 8: Langfuse Integration - DONE
- Epic 9: Gemini Provider - DONE
- Epic 13: Proactive AI - DONE
- Epic 14: Direct Memory Storage - DONE
- Epic 15: Extended MCP Tools - DONE
- Epic 16: Home Assistant Integration - DONE
- Epic 17: Cloud Logging (Loki) - DONE
- Epic 18: File Context Sharing - DONE
- Epic 23: Cost & Observability Hardening (Story 23.1) - DONE

### Remaining / Draft Epics
- Epic 6: Integration & Quality (Testing, E2E, Docs) - NOT STARTED
- Epic 10: Portfolio Management - DRAFT
- Epic 11: Real-time Status Updates - DRAFT
- Epic 12: Memory Storage Enhancements - DRAFT
- Epic 19: Adaptive Context & Persona Modulation - DRAFT (High Priority)
- Epic 20: Web UI - PARTIAL (scaffolded, needs completion)
- Epic 21: WhatsApp Integration - DRAFT
- Epic 22: Session Context Continuity - IN PROGRESS (P0) — 22.1/22.2/22.3 done; 22.2.1 follow-on (30-day TTL + history fetch) this wave; 22.4 reshaped (structured-aggregation only); 22.5 deferred
- Epic 24: Provider Modernization & Tech Debt - IN PROGRESS (P0) — 24.1 drafted (Gemini SDK migration + implicit prompt-cache adoption); kicked off after deprecation warning surfaced + Parminder-2 cost-cache analysis
- Epic 25: Telegram Rendering & Bot Reliability - IN PROGRESS (P1) — 25.1 drafted (LLM-emit-Telegram-HTML-direct architecture pivot); opened 2026-04-25 after the strip-helper's 3rd hotfix-patch surfaced as a code smell
- Story 4.2: Stock Trader Tool - Market Analysis - NOT STARTED
- Story 4.3: Stock Trader Tool - Personalized Recommendations - NOT STARTED

## Epic 22: Session Context Continuity

- Status: 22.1/22.2/22.3 done; 22.4/22.5 deferred
- Priority: P0
- Created: 2026-04-16
- Owner: Disha (product), Parminder (tech), David (impl), Harpreet (review), Murat (test)
- Doc: docs/epics/epic-22-session-context-continuity.md

### Story 22.1: Always-inject cached rolling summary
- Status: done (awaiting Ankit's review/merge)
- Assigned: David/Harpreet/Murat (Fenny-as-proxy this session)
- Priority: P0
- Size: S (~40 LOC)
- Dependencies: none
- Review Cycles: 0
- Acceptance Criteria:
  - [ ] `build_llm_context` injects cached `conversation:{id}:summary` as a system message regardless of token budget, when present
  - [ ] Injection framed as `[Earlier conversation summary]` (matches existing overflow path string)
  - [ ] No double-injection when overflow path also fires
  - [ ] Existing overflow/summary-refresh logic still works
  - [ ] Unit test covers: (a) summary present + short context injects once; (b) no summary cached → no injection; (c) overflow path still injects without duplication
- Notes: Already-maintained `conversation:{id}:summary` cache; current code injects only on >20k-token overflow. Pruning keeps context small so injection almost never fires — load-bearing gap.

### Story 22.2: Daily scratchpad `[CURRENT_DAY_CONTEXT]`
- Status: done (awaiting Ankit's review/merge); superseded by 22.2.1 (TTL+history) and 22.2.2 (free-form keys).
- Assigned: David/Harpreet/Murat (Fenny-as-proxy this session)
- Priority: P0
- Size: M (~200 LOC)
- Dependencies: none
- Review Cycles: 0
- Acceptance Criteria (as shipped post-22.2.2):
  - [ ] Redis key `daily_context:{user_id}:{YYYY-MM-DD Pacific}` with 30-day-rolling TTL (22.2.1)
  - [ ] Free-form keys validated by `^[a-z][a-z0-9_]{0,31}$`; per-day 20-key cap; per-key 2000-char cap (22.2.2)
  - [ ] New MCP tool `update_daily_context(user_id, key, value)` in `mcp_server/tools/` registered via `server.py`
  - [ ] New helper in `backend/api/daily_context.py` (sibling module) to read+format scratchpad for prompt
  - [ ] `build_system_prompt` renders `[CURRENT_DAY_CONTEXT]` section when scratchpad non-empty
  - [ ] `routes/stream.py` loads scratchpad and passes to `build_system_prompt`
  - [ ] Unit test covers: write → read round-trip; free-form key accepted (e.g., `house_hunting`); empty scratchpad gives no prompt section; blank value clears a key from prompt
- Notes: Original slot design (6 enum values) was generalized in 22.2.2 to accept any LLM-chosen semantic key. The previous names remain valid and are suggested as common examples.

### Story 22.2.1: 30-day rolling TTL + historical fetch tool + language sweep
- Status: done (awaiting Ankit's review/merge)
- Assigned: David/Harpreet/Murat (Fenny-as-proxy this session)
- Priority: P0
- Size: S (~150 LOC)
- Dependencies: 22.2
- Review Cycles: 0
- Acceptance Criteria:
  - [ ] TTL extended from "next Pacific midnight" to 30 days past the Pacific midnight of the date in the key (fixed per-key horizon, not rolling-on-write). Helper: `eviction_epoch_for_pacific_date(date_str)`.
  - [ ] New MCP tool `get_daily_context(user_id, date?, days_ago?)` registered via `server.py` — exactly one of `date` (YYYY-MM-DD) or `days_ago` (0-30) required. Returns `{status:success, scratchpad, found}` on hit, `{status:success, scratchpad:null, found:false}` on miss.
  - [ ] Prompt-injection behavior unchanged: `[CURRENT_DAY_CONTEXT]` still shows TODAY only. Past days accessible via the new tool only.
  - [ ] Prompt guidance updated: decision-tree references `get_daily_context` for past-day questions; explicit "say you don't have the scratchpad, don't hallucinate" when `found:false`.
  - [ ] Language sweep: "session-scoped" replaced with "day-scoped" / "today-only" (prompt) and "30-day rolling" (storage) across `daily_context.py` (mcp + backend), `prompts.py`, epic doc, and tests. "session-scoped" is gone from the codebase (except Epic 19's unrelated 2-hour TTL).
  - [ ] Unit tests cover: TTL computation for a given date (incl. DST boundaries), new tool validation (mutual exclusion, range, malformed date, future date), history fetch with hit / miss / corrupt JSON / Redis error, days_ago=0 equals today.
  - [ ] All previously-passing tests still green (459 backend + 478 mcp).
- Notes: 22.4 was considered for subsumption but **reshaped instead**: 22.4 remains P1 deferred and now scopes to structured-aggregation entries (workout counts, sleep averages, weight trend) — free-text daily state is wholly owned by the scratchpad post-22.2.1. Epic doc updated.

### Story 22.3: Rewrite `store_memory` prompt guidance
- Status: done (awaiting Ankit's review/merge)
- Assigned: David/Harpreet/Murat (Fenny-as-proxy this session)
- Priority: P0
- Size: S (prompt-only, ~50 LOC of prompt text)
- Dependencies: 22.2 (references scratchpad by name)
- Review Cycles: 0
- Acceptance Criteria:
  - [ ] `backend/api/prompts.py:271-320` `MEMORY_MANAGEMENT_SECTION` rewritten
  - [ ] Remove "background extraction handles this" framing
  - [ ] Replace with decision tree: day-scoped → scratchpad; permanent → store_memory; never "I don't know" without consulting `[CURRENT_DAY_CONTEXT]` then `[Earlier conversation summary]` (and `get_daily_context` for past-day questions, added in 22.2.1)
  - [ ] Existing unit tests in `backend/tests/unit/test_prompts.py` still pass (update assertions that probe the old text)
- Notes: Sequenced after 22.2 write (both touch prompts.py); 22.3 rewrites a bounded region so conflict risk is low.

### Story 22.2.2: Generalize scratchpad — drop slot enum, free-form keys
- Status: done (awaiting Ankit's review/merge)
- Assigned: David/Harpreet/Murat (Fenny-as-proxy this session)
- Priority: P0
- Size: S (~80 LOC)
- Dependencies: 22.2, 22.2.1
- Review Cycles: 0
- Acceptance Criteria:
  - [ ] `ALLOWED_SLOTS` enum and `DAILY_CONTEXT_SLOTS` tuple removed from prod code. MCP tool schema uses `pattern`/`maxLength` on `key`, not `enum`.
  - [ ] Parameter renamed `slot` → `key` in handler, schema, description.
  - [ ] Key validator `^[a-z][a-z0-9_]{0,31}$`. Rejects uppercase/whitespace/punctuation/emoji/leading-digit with a clear error message.
  - [ ] Per-day key-count cap (20 keys). NEW keys rejected at cap with `KEY_LIMIT_EXCEEDED` and list of currently-set keys for the LLM to consolidate into. Updates to existing keys always allowed.
  - [ ] `format_for_prompt` renders dict in insertion order; blank/empty/whitespace values stripped (serves as the CLEAR mechanism).
  - [ ] Prompt guidance in `MEMORY_MANAGEMENT_SECTION` updated: free-form snake_case, reuse-don't-proliferate, non-enum examples (house_hunting, trip_plans, debugging, pet_medication), clear via empty string.
  - [ ] Epic doc §22.2 rewritten to document 22.2.2 follow-on scope.
  - [ ] All pre-existing tests pass; new tests cover arbitrary keys, invalid key rejection, 20-key cap, insertion-order rendering, clear mechanism.
- Notes: No schema migration — prior keys (meals / schedule / etc.) all pass the new validator. 22.4 rationale unchanged (numeric aggregation is still 22.4's job, scratchpad is still prose).

### Story 22.4: update_daily_log tool — DEFERRED + RESHAPED (not this wave)
- Status: draft (reshape landed 2026-04-16; see epic doc §22.4)
- Scope narrowed: structured entries only (workout/weight/sleep/meal_macros/mood_score). Free-text daily state removed — owned by scratchpad post-22.2.1.
- Priority: P1. Unblocks when Ankit starts serious recomp aggregation queries.
### Story 22.5: Pre-generation memory RAG prefetch — DEFERRED (not this wave; 22.5 revised finding on bus re: memory_client.stream_message)

## Epic 23: Cost & Observability Hardening — DONE

- Status: DONE (Story 23.1 pushed by Ankit 2026-04-25; +24h/+7d/+30d watchdog calendar live, owned by Murat)
- Priority: P0
- Created: 2026-04-25
- Completed: 2026-04-25
- Owner: Disha (product), Parminder (tech), David (impl), Harpreet (review), Murat (test)
- Theme: Make Annie's spend numbers trustworthy enough to base budget alarms / soft caps / auto-shutoff on. Reconciliation 2026-03-26→04-25 found Annie recorded $12.19 vs Google billed ~$100 (-88%). Cost tracking is the load-bearing input for any future cost-safety machinery; right now it would fail silently long before any guardrail fires. This epic absorbs follow-ons: budget alerts, soft-cap throttling, monthly reconciliation tooling, and per-user cost attribution.

### Story 23.1: Fix provider token counting in streaming providers
- Status: DONE (pushed 2026-04-25 — watchdog calendar +24h/+7d/+14d/+30d in bus)
- Assigned: David (implementation)
- Priority: P0
- Size: M (~250-400 LOC across 3 prod files + 1 reconciliation script + tests)
- Dependencies: none (independent of David's Class C cross-provider history bug; can land in either order — see Notes for sequencing guidance)
- Review Cycles: 0
- User Problem: Annie's recorded Gemini spend is ~88% lower than Google's actual bill ($12.19 vs ~$100 over 30 days). Output tokens are recorded as the **stream-chunk count** (a 1,031-char real reply was logged as `completionTokens: 16`, ~50x undercount). Error/429 paths short-circuit tracing entirely (last 3 days have ZERO Gemini observations despite the cap-trip burst that pushed Google to $100). This makes cost data unusable as the basis for budget alarms, soft caps, or any future auto-shutoff. Without trustworthy cost numbers, Ankit cannot safely scale Annie's usage or extend access to others.

- Acceptance Criteria:
  - [ ] **AC1 — Gemini token source corrected.** In `backend/api/providers/gemini_provider.py`, both Langfuse-tracking blocks (lines ~685-740 streaming-text path; lines ~1034-1086 function-call path) read `prompt_tokens` and `completion_tokens` from the final stream chunk's `usage_metadata` (`prompt_token_count`, `candidates_token_count`). Lines 641 and 1037 (`token_count += 1` per chunk) are removed or repurposed for non-cost telemetry only. Verify: a unit test feeding a mock stream where the final chunk has `usage_metadata(prompt_token_count=1234, candidates_token_count=567)` must produce a Langfuse generation with `usage.input=1234, usage.output=567`, regardless of how many chunks were emitted.
  - [ ] **AC2 — Pre-stream `count_tokens()` round-trip removed.** The separate `client.count_tokens()` call at `gemini_provider.py:~689` is deleted. Input tokens come exclusively from the final chunk's `usage_metadata.prompt_token_count`. Verify: grep for `count_tokens` in `gemini_provider.py` returns zero matches in the streaming code path; existing tests still pass.
  - [ ] **AC3 — Gemini fallback when `usage_metadata` absent.** If the final stream chunk lacks `usage_metadata` (defensive — SDK contract change, malformed response, mid-stream abort), the generation is still emitted to Langfuse with `usage=None` and a structured log line `event="usage_metadata_missing"` at WARNING level including conversation_id, model, and chunk_count. We do NOT fall back to chunk-counting for cost. Verify: unit test with a stream that never emits `usage_metadata` produces (a) a Langfuse generation with null usage, (b) a single WARNING log with `event="usage_metadata_missing"`, (c) no exception raised to caller.
  - [ ] **AC4 — ChatGPT `stream_options.include_usage=true` enabled.** In `backend/api/providers/chatgpt_provider.py` (the streaming completion call site, currently around line 543's usage block), the OpenAI streaming request sets `stream_options={"include_usage": True}`. The final chunk's `usage` block is read for `prompt_tokens` and `completion_tokens`. The chunk-count fallback at line 543 (`usage.get("completion_tokens", token_count)`) is replaced with the same null-and-warn behavior as AC3. Verify: unit test with mock OpenAI stream emitting a final chunk with `usage={prompt_tokens:1000, completion_tokens:500}` produces a generation recorded with those exact values; mock stream WITHOUT `usage` produces null-usage generation + WARNING `event="usage_metadata_missing"`.
  - [ ] **AC5 — Error-path tracing for 4xx/5xx.** When the provider raises (rate limit / quota / network / 5xx), Langfuse still receives a `generation.end()` call before the exception propagates. The generation has: `level="ERROR"`, `status_message=<exception class + truncated message>`, `usage` populated from `usage_metadata` if the SDK delivered it before the failure (Gemini's 429 sometimes does after prompt-processing), else null. The orchestrator's existing exception handling is unchanged — this is a `try/finally` around `generation.end()`, not a swallowing catch. Verify: integration test mocks Gemini raising `ResourceExhausted("quota exceeded")` after one chunk; assert one Langfuse generation exists with `level="ERROR"` and the model name set. Same test for ChatGPT 429.
  - [ ] **AC6 — The Apr-04-shaped placeholder traces eliminated.** Parminder identified a separate broken path: 17 traces on Apr 4 (and similar on Apr 6) with `in=10, out=5-28` for every call — likely a placeholder-record path or a silent `count_tokens()` failure landing in the `except: pass` branch around line 1034. Inspect both Langfuse-tracking blocks; ensure no code path records hardcoded or default-zero token counts as if they were real. If the path is identified, fix it; if it's already covered by AC1/AC2/AC5, document why in the PR description. Verify: search the next 7 days of `release=annie-prod` Langfuse traces for any generation with `usage.input < 50 AND output_chars > 200` — should return zero hits. Manual spot-check, not CI.
  - [ ] **AC7 — Reconciliation tooling exists.** A new script at `scripts/cost_reconciliation.py` (Python, runnable inside any Annie service container or standalone) takes a date range and produces a per-day report comparing: Annie's Langfuse-recorded cost (REST API: `GET /api/public/observations` filtered by `type=GENERATION` and date range, summed by `cost.total_cost`), per-model token breakdown, and a column for "Google billed (manual entry)" that the operator fills from the Google Cloud Console billing detail. Output: markdown table writeable to stdout or `reports/cost_YYYY-MM-DD_to_YYYY-MM-DD.md`. The script does NOT need to integrate with Google Cloud Billing API in this story — that's a separate followup. Verify: running `python scripts/cost_reconciliation.py --start 2026-04-01 --end 2026-04-25 --output stdout` returns a markdown table; manual smoke test by Parminder.
  - [ ] **AC8 — Manual reconciliation checkpoint scheduled.** Once 23.1 is merged, Disha posts a calendar reminder for +7 days, +14 days, and +30 days post-merge to run the script and compare to Google's billing detail. Target variance at +30 days: <10% (Parminder's original AC5). At +7 and +14, variance is informational — we expect the gap to be noticeably narrower than 88% even at +7 days. If variance stays above 25% at +30 days, open a follow-up story (Story 23.2) to dig into remaining gap sources (untraced retries, count_tokens billing, etc.). This AC is satisfied by the calendar reminder being set; the reconciliation itself is done in followup work.
  - [ ] **AC9 — All existing provider tests still pass.** `backend/tests/unit/providers/` and `backend/tests/integration/` green. New tests added per ACs above.
  - [ ] **AC10 — No log-spam regression.** New WARNING-level `event="usage_metadata_missing"` does not fire for >1% of requests in the first 24h post-merge under normal traffic. If it does, that means SDK behavior is different than assumed — escalate before relying on cost data. Verify: spot-check Loki / docker logs at +24h, count of `event="usage_metadata_missing"` divided by total chat requests.

- Edge Cases:
  - Stream aborted client-side mid-response (Telegram disconnect): final chunk never arrives → AC3 fallback fires (null usage + WARNING). Acceptable — partial costs untracked but not bogus.
  - Gemini SDK retries internally and our code only sees the final result: `usage_metadata` will reflect the successful attempt's tokens, not the retried-and-failed attempts. Google bills us for ALL attempts. This is a known residual gap that the reconciliation script (AC7) will surface; not in scope for this story to eliminate.
  - Gemini 429 returns `usage_metadata` with `prompt_token_count > 0` but `candidates_token_count == 0` (prompt processed, generation refused). AC5 should record this faithfully — this is real billed prompt-processing.
  - ChatGPT-5 + tools: when the response is a tool call, the `completion_tokens` includes the tool-call JSON tokens. AC4 should still capture this correctly via `usage.completion_tokens`.
  - `release=annie-dev` vs `release=annie-prod`: tooling AC7 should accept a `--release` filter so dev traffic doesn't pollute prod reconciliation.

- Notes:
  - **Sequencing vs David's Class C story (cross-provider history corruption).** Independent — different files, different test surfaces. This story touches `gemini_provider.py:~640-740, ~1034-1086`, `chatgpt_provider.py:~540-560`, plus a new script. David's story touches `chatgpt_provider.py` message-building (different region) and likely `state.py` persistence shape. Low merge-conflict risk. Parminder's call on which lands first; my recommendation is **23.1 first** because (a) accurate cost data is a prerequisite for any future budget guardrail and (b) it's lower-risk (instrumentation only, no conversation-correctness changes).
  - **Why this is M not S.** AC1/AC2/AC4 are surgical (~30 LOC). AC5 (error-path tracing) requires `try/finally` discipline around generation.end() at multiple call sites and careful coordination with existing exception handling — easy to break. AC6 requires investigation. AC7 is a new ~150-LOC script. Tests are ~100-150 LOC. Total budget: ~250-400 LOC including the script.
  - **Why a new epic, not Story 8.X.** Epic 8 was about wiring up Langfuse infrastructure (one-time setup, DONE). This kicks off a budget-safety theme: the next stories under Epic 23 will be soft-cap alerts (e.g., warn at $50/month), per-user cost attribution, the Google Cloud Billing API integration that AC7 stops short of, and possibly an auto-shutoff. Grouping them under their own epic keeps the cost-safety thread coherent.
  - **Numbers to keep in mind.** Recorded $12.19 ≈ input cost only. Adjusted estimate with proper output counting ≈ $22. Remaining $78 gap is hypothesized (per Parminder) to come from untraced SDK-internal retries, count_tokens billing, and the cap-trip burst that backoff suppressed from tracing. AC1-AC5 should close most of this; AC7+AC8 quantifies the residual.
  - **Open question for Parminder — RESOLVED 2026-04-25.** Gemini `count_tokens()` is **likely free** (Google docs indicate count_tokens is a metadata endpoint that doesn't run the model — no input or output token billing). However, the codebase makes a SEPARATE network round-trip per stream success path (line 689) and per "ended without STOP" path (line 1040) to call `model.count_tokens(gemini_messages)` — that's a real RPC even if not metered against inference. AC2 (delete the round-trip) is the correct call regardless: (a) we're already going to have `prompt_token_count` on the final chunk's `usage_metadata` for free, (b) eliminates a latency hit and a failure mode (the `except: pass` at line 1041 silently swallows count_tokens errors, which is hypothesis #2 for the Apr-04 placeholder traces), (c) any non-zero possibility of metering goes away. So AC2 stays as written, AC8 still verifies the residual at +30. Net: don't block David on this; the architecture is right.

  - **Implementation note from Parminder for David (AC5 shape).** Wrap `generation.end()` in a `try/finally` at the call site for now — do NOT extract a context manager in this story. Rationale: the streaming path is already a deeply-nested generator (`async for chunk in stream` inside `while tool_iteration < max`), and threading a context manager through that without changing yield semantics is a refactor wider than 23.1's scope. A localized `try: ... finally: generation.end(...)` around each of the four affected blocks (gemini success, gemini ended-without-stop, gemini error, chatgpt success+error) keeps the diff bounded and reviewable. If we find ourselves writing the same finally three times, Harpreet flags it in review and we extract in a 23.1.1 follow-up. **For AC3/AC4 null-fallback shape: use `usage=None` (i.e., omit the usage dict entirely from `generation.end()`) — Langfuse v2 accepts a missing usage and renders the generation with no token/cost data, which is exactly the signal we want ("we don't know"). Do NOT pass `usage={"input":None, "output":None}` — the SDK will likely coerce to 0 and re-introduce false-zero cost rows downstream.**

## Epic 24: Provider Modernization & Tech Debt

- Status: IN PROGRESS (24.1 drafted — awaiting Parminder ready-approval)
- Priority: P0
- Created: 2026-04-25
- Owner: Disha (product), Parminder (tech), David (impl), Harpreet (review), Murat (test)
- Tech Spec: `.claude/scrum/docs/tech-specs/epic-24-spec.md` (Parminder-1, lifted as the design source of truth for 24.1)
- Theme: Two motivations land in one move. **(1) Forced hand:** `google.generativeai` is deprecated — backend logs are emitting the FutureWarning on every cold start, the package is no longer receiving bug fixes, and the urgency is months not days but the runway is finite. **(2) Cost dividend we can't capture today:** the new `google.genai` SDK on `gemini-3.1-pro-preview` enables **implicit prompt caching at a 90% discount on cached input tokens** (Parminder-2's analysis — Annie's ~5KB system prompt + profile + scratchpad is identical across turns of a conversation, so a 60–80% input-cache hit rate after the first turn is realistic). Migrating without instrumenting the cache hit-rate would mean we'd never know if the discount actually landed. This epic does the migration AND verifies the cost gain end-to-end. Future stories under this epic absorb other deferred provider modernization work (thinking-budget tuning per prompt class, batch API for the proactive worker, native parallel tool calls, retire `gemini_tool_adapter.py` if `FunctionDeclaration.from_callable()` shapes the call site cleanly).

### Story 24.1: Migrate Gemini provider from `google.generativeai` to `google.genai` + capture implicit prompt-cache discount
- Status: done (P0 hotfix cycle — David forward-fix APPROVED by Harpreet 2026-04-25 cycle 1, then APPROVED by Murat 2026-04-25 cycle 1; SDK 1.73.1, PTB 22.7, thought_signature capture verified end-to-end against real Gemini API, 738/738 backend tests pass, AC9 clean-env satisfied, live smoke 3-of-3 happy-path scenarios PASS — plain text + single tool + multi-turn tool persistence — zero 400s, zero `automatic_function_calling` log noise, zero ERROR/CRITICAL across services in past 1h, six distinct thought_signatures captured per-turn over the smoke run; e2e regression test gated behind RUN_LIVE_E2E=1 — implementation-complete, awaiting Ankit's commit + push + push-time deeper smoke per Murat's runbook)
- Assigned: David (implementation)
- Priority: P0
- Size: M (~340 LOC churn, ~+170 net — single provider rewrite + test re-fixturing + cache-hit logging extension. Parminder-1's tech-spec budget. One day implementation, one day testing + manual smoke.) Hotfix added ~30 LOC delta (signature capture + log + new regression test).
- Dependencies: none (Epic 23 / Story 23.1 merged 2026-04-25 — prerequisite resolved)
- Blocks: none known (provider external contract is stable; cross-cutting changes are zero)
- Review Cycles: 1
- User Problem: Two interlocking pains. **(a) The SDK we depend on is officially dead** — `google.generativeai` 0.8.6 is end-of-support, no more bug fixes, FutureWarning at every backend cold start. We're on borrowed time. **(b) We're leaving a measurable cost win on the table** — Annie's system prompt + profile fields + daily scratchpad are identical across every turn of a conversation. The new SDK on `gemini-3.1-pro-preview` caches that input at a 90% discount automatically (implicit caching, no code beyond the migration). At Annie's recently-reconciled $100–150/month Gemini run-rate, a 60–80% input-cache rate = a material reduction with zero feature debt. Migrating without verifying the discount actually landed in Langfuse would waste the dividend. This story lands both: the deprecation fix AND the cost dividend, instrumented end-to-end so we can see in Langfuse traces that caching is working.

- Acceptance Criteria:
  - [ ] **AC1 — Dependency swap.** `backend/requirements.txt` drops `google-generativeai>=0.3.0` and adds `google-genai>=1.0` (or whatever stable is current at story-pickup time). `make rebuild` succeeds. `grep -rn "google.generativeai\|google.ai.generativelanguage" backend/` returns zero matches across prod and tests.
  - [ ] **AC2 — Client construction migrated.** `backend/api/providers/gemini_provider.py:91` `genai.configure(api_key=...)` is replaced with `self.client = genai.Client(api_key=...)`; `genai.GenerativeModel(...)` instantiation at line 94 is gone. Generation config and safety settings move to per-call `types.GenerateContentConfig(...)`. No module-global `genai.configure` state remains.
  - [ ] **AC3 — Async streaming chat migrated.** `model.start_chat(history=...)` + `chat.send_message(msg, stream=True, tools=tools_config)` is replaced with `client.aio.chats.create(model=..., history=..., config=...)` + `chat.send_message_stream(message=msg)`. The `for chunk in response` loop becomes `async for chunk in response`. (Resolves Parminder-1 open question #1: AC12 included — full async path; sync-iter wrapping inside an async function is gone.)
  - [ ] **AC4 — `automatic_function_calling=AutomaticFunctionCallingConfig(disable=True)` set explicitly.** This is the silent-footgun tripwire: the new SDK's default is to auto-execute Python callables, bypassing the MCP layer. Disable is non-negotiable. Unit test asserts that when an MCP-shaped tool emits a `function_call` part, the provider yields a `tool_call_started` event to the orchestrator (i.e., MCP path taken) and does NOT auto-execute.
  - [ ] **AC5 — Function-response builder swapped.** `glm.Part(function_response=glm.FunctionResponse(...))` at `gemini_provider.py:1017–1023` is replaced with `types.Part.from_function_response(name=..., response={...})`. The function-local `from google.ai import generativelanguage as glm` import is deleted. Two-turn integration test: tool call → tool response (built via the new helper) → second turn produces `STOP`, NOT `MALFORMED_FUNCTION_CALL` (finish_reason=10). This catches the wire-shape regression that Story 9.3's MALFORMED retry would otherwise mask as a flake.
  - [ ] **AC6 — `convert_proto_to_dict` walker deleted.** The 60-line recursive helper at `gemini_provider.py:666–705` is removed. `function_call.args` is consumed as a plain Python `dict` (Pydantic-backed in the new SDK, not proto). New unit test feeds deeply-nested args (object → array → object) end-to-end and asserts `json.dumps(fc["args"])` round-trips cleanly through the persistence boundary.
  - [ ] **AC7 — Safety enums + settings migrated.** `from google.generativeai.types import HarmCategory, HarmBlockThreshold` becomes `from google.genai import types`; `HarmCategory.HARM_CATEGORY_*` and `HarmBlockThreshold.BLOCK_*` references move to `types.HarmCategory.* / types.HarmBlockThreshold.*`. Safety settings move from `dict[HarmCategory, HarmBlockThreshold]` to `list[types.SafetySetting(category=, threshold=)]` inside `GenerateContentConfig`. Test asserts settings actually reach the SDK call (closes Murat coverage gap #1: no test verifies safety_settings application today).
  - [ ] **AC8 — Typed Pydantic field access replaces defensive `getattr` chains.** All `getattr(part, 'function_call', None)` / `hasattr(part, ...)` patterns in `gemini_provider.py` (notably around lines 587–597) are replaced with typed Pydantic field access on `Part`, `Candidate`, `UsageMetadata`. (Lifts Parminder-2's recommendation directly; this is most of the maintainability dividend from the migration.)
  - [ ] **AC9 — Test patches re-targeted to provider-owned attributes (silent-pass elimination).** All `@patch('api.providers.gemini_provider.genai.configure')` / `@patch('...genai.GenerativeModel')` decorators in `test_gemini_provider.py` (14 sites), `test_story_23_1_token_counting.py` (2 sites in `_build_gemini_provider`), and `test_file_multimodal.py` (1 site) are replaced with patches against `provider.client` / provider-owned attributes (or constructor-time injection of a fake client). **Verification:** the full backend test suite passes in a container where `google-generativeai` is uninstalled (`pip uninstall google-generativeai && pytest backend/tests`). This is the only true catch for Murat's silent-pass class.
  - [ ] **AC10 — Story 23.1 invariants preserved.** All 11 tests in `test_story_23_1_token_counting.py` pass after re-fixturing for the new chunk shape: `test_gemini_provider_uses_usage_metadata`, `test_gemini_provider_omits_usage_when_metadata_absent`, `test_gemini_provider_no_count_tokens_call`, `test_gemini_error_path_traces_generation`, `test_gemini_mid_stream_429_records_real_prompt_tokens`, plus the ChatGPT counterparts. Story 23.1's +24h/+7d/+30d watchdog calendar (Loki `usage_metadata_missing<1%`, Langfuse Apr-04 placeholder check) remains valid post-migration.
  - [ ] **AC11 — Coverage gaps closed in-flight (cheaper than retrofit).** Five named gaps from Murat's audit gain real tests as part of this story: (1) safety_settings actually reach the SDK call, (2) `function_call.name` / `function_call.args` shape on streaming chunks, (3) `thought_signature` preservation across multi-turn tool calling, (4) `tool_config` / `function_calling_config` mode flow, (5) `generation_config` (max_output_tokens, temperature) actually flowing into the SDK call. Each is one focused unit test using the new typed fakes.
  - [ ] **AC11.5 — Typed-exception ContextLengthError detection preserved.** The substring matcher at `gemini_provider.py:1318` (`"resource_exhausted" in error_str ... "token" ... "limit"`) is updated to also handle `google.genai.errors.ClientError` and `google.genai.errors.APIError` (whatever the new SDK's typed exception classes are). Approach: cast the exception via `getattr(e, 'message', str(e))` AND inspect `getattr(e, 'code', None)` / `getattr(e, 'status', None)` (e.g., `RESOURCE_EXHAUSTED` enum or 429 numeric) before falling back to substring. Unit test: raise the new typed exception with a context-overflow message, assert `ContextLengthError` is raised (not `ProviderError`). Same for the rate-limit branch at line 1329. Promoted from edge-case to AC because Disha's instinct was right — the typed-exception migration silently breaks today's overflow handling, and Story 9.x's overflow-recovery path is load-bearing for the 1M-token edge case in Murat's smoke plan.
  - [ ] **AC12 — Cost-calc parity.** `provider.calculate_cost({"prompt_tokens": 1000, "completion_tokens": 500, "cached_tokens": 0})` returns the same dollar figure pre- and post-migration. One-line test (Annie's cost table is keyed by model-name string, SDK-independent — but verify, don't assume).
  - [ ] **AC13 — Multimodal smoke survives.** An `inline_data` image part still reaches the model via the new SDK contents path. Re-uses `test_file_multimodal.py::TestGeminiMultimodalIntegration` scaffolding. Closes Murat's silent-pass concern on the `parts: [{inline_data: ...}]` shape — the test must assert against the NEW SDK call surface, not the old `mock_model` MagicMock.
  - [ ] **AC14 — Retire-or-keep decision on `gemini_tool_adapter.py`.** Evaluate whether the 513-LOC OpenAI↔Gemini schema converter can be retired given the new SDK's typed `FunctionDeclaration.from_callable()` shapes the call site cleanly enough. **If yes:** delete `gemini_tool_adapter.py` and its 24 tests in `test_gemini_tool_adapter.py`, port any still-needed helpers inline. **If no:** document the specific reason in the tech spec (e.g., MCP boundary requires OpenAI-shaped schemas that neither SDK can synthesize from). David's call after the migration shape is in; do not block on this — flexibility AC, not gate.
  - [ ] **AC15 — Implicit prompt-cache instrumentation (the cost-gain enforcement AC).** Cost tracking reads `cached_content_token_count` from `usage_metadata` on each generation and emits it to Langfuse alongside the existing `input` / `output` token counts. The Langfuse generation has a typed field (`usage.input_cached` or equivalent — match Langfuse v2 SDK's cached-token field name) that survives the trace-export pipeline. **Verifiable cost-gain target (calendar checkpoint, not CI):** at +7 days post-merge under normal traffic, Langfuse traces show `cached_content_token_count > 0` on **≥50% of `gemini-3.1-pro-preview` generations beyond the first turn of a conversation** (first-turn generations get a free pass — the cache has to populate before it can hit). At +30 days, the trailing-30-day cumulative input-token billed cost (per the `cost_reconciliation.py` script from Story 23.1) shows **a ≥40% reduction vs the trailing-30-day baseline before merge** (input cost only — output costs aren't expected to move). If either threshold is missed, open Story 24.2 to investigate (likely either the cache isn't kicking in due to prompt drift, or the `cached_content_token_count` field isn't reaching Langfuse). This AC is **the binding directive from Ankit** — implicit caching is a HARD requirement, not a follow-up.
  - [ ] **AC16 — Deprecation warning gone.** Post-merge `make logs SERVICE=backend` returns zero matches for `"google.generativeai package has ended"` AND zero matches for `"deprecat"` originating from `google.generativeai` import paths. Verifies the legacy package isn't being import-pulled by some transitive dep we missed.

- Definition of Done (DoD — separate from ACs, signed off before Ankit pushes):
  - All 16 ACs above met and verified.
  - Test counts non-decreasing: backend ≥708, mcp ≥542 (Story 23.1 baseline). New tests added (AC4, AC5, AC6, AC7, AC8, AC11, AC13, AC15) increment, not replace.
  - **Murat's 8-step manual smoke plan executed in dev (uncommitted) by David, verified via Telegram + Langfuse by Murat:** (1) plain-text turn (Langfuse `input>0, output>0, level=DEFAULT`), (2) tool-calling turn (`internet_search`/`web_search` — SSE `tool_call_started`/`completed`, mcp_tool_call span), (3) multi-turn tool persistence (same conversation, second turn — `thought_signature` preserved, no Gemini 400), (4) multimodal image (Telegram-share, response references image), (5) document upload (PDF + summarize), (6) mid-stream 429 simulation (only if Ankit drops cap to $1 — `level=ERROR` Langfuse generation with `prompt_token_count > 0` per Story 23.1), (7) context overflow (1M-token message — `ContextLengthError` → compaction → retry/fallback), (8) API-key failover (break `GOOGLE_API_KEY`, verify auto-fallback to ChatGPT/Grok). **Manual smoke is DoD because none of these reproduce in CI** (no live Gemini integration tests today — `test_gemini_mcp.py` is all `pytest.skip()`).
  - **Murat's +24h post-push watchdog signed off:** (a) `usage_metadata_missing` rate <1% in Loki (Story 23.1 carry-forward), (b) zero log entries matching `"google.generativeai"` or `"deprecat"`, (c) `cached_content_token_count > 0` visible in Langfuse on at least one `gemini-3.1-pro-preview` generation (proves the AC15 plumbing works; threshold check is at +7d).

- Edge Cases:
  - `chunk.candidates[0].content.parts` empty on intermediate chunks (text-only deltas) — current code already guards; new typed-Pydantic access path must too.
  - First turn of a conversation has no cached content (`cached_content_token_count == 0`) — expected; AC15's threshold excludes first turns.
  - Cache invalidation when system prompt changes between turns (e.g., scratchpad updated, profile field changed) — implicit caching tolerates partial-prefix mismatch by hashing prefix segments; if our cache hit rate drops sharply after a known prompt change, that's diagnostic signal not a regression.
  - SDK's typed exception wrapping: `google.genai` may raise a typed `google.genai.errors.ClientError` instead of the old SDK's exception class. Substring matching in `gemini_provider.py`'s `ContextLengthError` detection (`"RESOURCE_EXHAUSTED" + "token" + "limit"`) must still work — new `__str__` shape may differ.
  - `client.aio.chats.create()` async-iter mixing: do NOT mix sync `for chunk in response` and async `async for chunk in response` in the rewrite. One slip and chunks aren't yielded.
  - Multi-turn loop: the new `chats.create` should preserve `thought_signature` parts via `history` the way `start_chat()` did. **Verify in implementation, not in spec.**

- Notes:
  - **Tech spec is the design source of truth.** `.claude/scrum/docs/tech-specs/epic-24-spec.md` (Parminder-1, lifted as-is). David should read sections 2 (API surface diff), 4 (hard-cut migration strategy), 5 (top 5 risks), 7 (sub-task breakdown), 8 (architectural decisions). Section 10 (dependency on 23.1) is now historical record — prerequisite is satisfied.
  - **Hard-cut over env-gating** (Parminder-1 Decision 1). Single-user system, low blast radius, simpler PR. Rollback = `git revert` + `make rebuild`. Don't introduce a `GEMINI_SDK=legacy|new` flag.
  - **Don't get distracted** (Parminder-2 explicit guard-rails, lifted): do NOT adopt AFC during this migration (Annie's MCP loop demands the disable flag); do NOT introduce the Files API for chat (breaks the process-and-discard invariant from Epic 18); do NOT swap Tavily/Brave for Gemini-native search grounding (architecture decision, not a migration win); do NOT add `ThinkingConfig` budgets per prompt class (good follow-up, not migration scope); do NOT introduce a `_call_sdk` abstraction layer "in case we migrate again" (YAGNI).
  - **Resolved open questions from Parminder-1 (3 of them):**
    - *AC12 (full async migration)* — Parminder said yes, my read agrees: yes. Folded into AC3.
    - *AC13 (cached-token logging)* — Per Ankit's binding directive, this is mandatory and MORE than just logging — it's the cost-gain enforcement AC with calendar checkpoints. Folded into AC15.
    - *Murat's 8-step smoke plan as ACs vs DoD* — agree with Parminder: DoD. Manual checks don't reproduce in CI.
  - **`gemini_tool_adapter.py` decision is David's call, not pre-judged** (AC14). Parminder-2 said retire if shape works; David sees the actual shape after the migration is in. Flexibility AC, not gate.
  - **Cost-gain target rationale.** Two thresholds, both calendar-checkpoint (Langfuse + cost-recon script run, not CI):
    - +7d: `cached_content_token_count > 0` on ≥50% of `gemini-3.1-pro-preview` generations beyond first-turn (proves caching is firing).
    - +30d: trailing-30d cumulative **input** cost reduction ≥40% vs trailing-30d baseline (proves the discount is landing in $$, not just in tokens). Both miss → open 24.2 for investigation.
  - **Sequencing.** Now that Epic 23 / Story 23.1 is pushed: Disha drafts → Parminder ready-approves → David implements (uncommitted) → Harpreet reviews → Murat tests → manual smoke → Ankit pushes. Standard Annie pipeline.

- Parminder approval (2026-04-25) — implementation steer for David:
  - **Singleton client, not per-request.** Construct `self.client = genai.Client(api_key=self.api_key)` once in `GeminiProvider.__init__` and reuse for every `stream_chat_completion` call. Per-request construction creates a new httpx async client + auth context per call — wasteful, and breaks any future connection pooling Google's SDK adds. The API key is constant per provider instance (even with `model_override`, the credential stays the same), so a singleton is safe. If we ever need per-call credentials (we don't), revisit.
  - **AFC-disable belongs in a config builder, not at every call site.** Add `_build_generate_content_config(self, tools, ...) -> types.GenerateContentConfig` as a private method that ALWAYS sets `automatic_function_calling=types.AutomaticFunctionCallingConfig(disable=True)` regardless of caller. Call sites pass tools/system_instruction; the disable flag is non-overridable. AC4's unit test should patch the SDK's chats.create call, capture the `config` argument, and assert `config.automatic_function_calling.disable is True` — that's the tripwire. Plus a second behavioral test: arm a python-callable tool spec, run a turn, assert the callable was NEVER invoked and the MCP `tool_call_started` event was emitted instead.
  - **Cache-token field — extend `_extract_usage_metadata`, do NOT add a parallel helper.** The 23.1 helper already returns `Optional[Dict[str, int]]`. Add a third key `cached_tokens` (read from `getattr(usage_md, 'cached_content_token_count', None)`, default 0 if missing — not None — because the field is semantically "we know it was zero" on Gemini 1.5 / first-turn, not "we don't know"). Then plumb `cached_tokens` through `last_usage` into the existing Langfuse `generation.end(usage=...)` payload as a fourth field. Langfuse v2's `usage` accepts arbitrary keys; verify the field surfaces in the trace UI as part of the +24h DoD watchdog. Do NOT introduce a separate `_extract_cache_metadata` — keeps the 23.1 invariant test surface intact.
  - **`gemini_tool_adapter.py` — KEEP, do NOT retire (strong steer, AC14 resolution).** The 513-LOC OpenAI↔Gemini schema converter exists because the MCP boundary delivers tools as OpenAI-shaped dicts (Annie's universal tool format across Grok/ChatGPT/Gemini), and `FunctionDeclaration.from_callable()` only works when you have a Python callable to introspect — Annie has dicts. Migrating to `from_callable()` would mean: (a) synthesizing fake Python callables from the MCP-delivered dicts (uglier than the current converter), (b) breaking the cross-provider tool format. The converter is the right boundary primitive; the migration doesn't change the ergonomics. **Document the decision in the PR description, mark AC14 satisfied with reason "MCP boundary delivers OpenAI-shaped dicts; `from_callable` requires Python callables; the adapter is the right primitive."** Save the 513 LOC + 24 tests for a different day.
  - **`thought_signature` preservation across `chats.create`** — verify in implementation, not in spec. The new SDK's `chats.create(history=...)` is documented to preserve `thought_signature` parts the way `start_chat()` did, but the migration is the wrong place to discover otherwise. AC11 sub-bullet #3 already requires a unit test for this — that test is the verification. If it fails, fall back to manually threading `thought_signature` parts through `history` between turns; document in the PR.
  - **Retries / backoff** — the new SDK has built-in retry with exponential backoff for transient failures (5xx, network blips). Annie's existing 2h cap-trip backoff lives in Redis at the orchestrator level (key `gemini:cap_tripped:{user_id}` or similar) and is keyed off the `RateLimitError` we raise from the provider. The two don't conflict: SDK retries happen below the provider's exception-translation layer; Annie's backoff fires on the typed `RateLimitError` we propagate up. **No change needed.** If David sees the SDK swallowing 429s into successful responses (it shouldn't — 429 is non-retryable by default), flag it before merge.

## Epic 25: Telegram Rendering & Bot Reliability

- Status: IN PROGRESS (25.1 drafted — awaiting Parminder ready-approval; blocked on Ankit's streaming-approach decision before Parminder can sign off)
- Priority: P1
- Created: 2026-04-25
- Owner: Disha (product), Parminder (tech), David (impl), Harpreet (review), Murat (test)
- Theme: The Telegram bot's text-rendering layer keeps acquiring patches. Today's MarkdownV2-strip hotfix is the **third** patch on the same `markdown_to_telegram_html` helper (escape-set coverage gaps detected after the LLM started reaching for new MarkdownV2 chars). Each patch is small, but the pattern says the architecture is wrong: we're transforming LLM output through MarkdownV2 → strip-escapes → HTML, when we could have the LLM emit Telegram's HTML allowlist directly and skip the transform layer entirely. This epic owns that pivot and opens space for other bot-reliability stories that today get squeezed in as one-off hotfixes (Telegram rate-limit handling, message-too-long chunking, edit-message race conditions, parse-mode failure fallback, voice/media-sender path consistency). Distinct from Epic 24 (provider SDK modernization) — different layer, different failure modes, different test surface.

### Story 25.1: Replace MarkdownV2-strip pipeline with LLM-emit-Telegram-HTML-direct
- Status: drafted (awaiting Parminder ready-approval; **gated on Ankit resolving Open Question #1 — streaming approach (a) vs (b)** before Parminder signs off)
- Assigned: unassigned
- Priority: P1
- Size: L (~2 days realistic per David-2's evaluation — prompt change ~10 LOC + defensive sanitizer ~150 LOC + streaming-path refactor 50–150 LOC depending on (a) vs (b) + prompt-regression test sweep + cross-provider compliance testing on Gemini/ChatGPT/Grok-4. Streaming-approach decision dominates the bottom of the range.)
- Dependencies:
  - **HARD**: today's `markdown_to_telegram_html` hotfix on `develop` must be PUSHED before this story starts (so production isn't visibly broken while 25.1 is in flight). The hotfix is the safety net during the migration.
  - **SOFT**: Story 24.1 (`google.genai` migration) should land first. The SDK swap may change how Gemini responds to the new prompt instruction — ideally the new prompt is validated against the migrated SDK, not the deprecated one. If 24.1 slips materially, 25.1 can proceed against current SDK with a re-validation gate post-24.1.
- Blocks: none known. The change is bot-side rendering; the LLM contract is "your output goes to Telegram with parse_mode=HTML" — provider-agnostic.
- Review Cycles: 0
- User Problem: Annie's Telegram bot has been patched three times for MarkdownV2-escape-strip coverage gaps (most recently today: 7 missing escape chars caused literal backslashes to leak into rendered Telegram messages — `\~75 cal \| 6g protein \> goal` instead of `~75 cal | 6g protein > goal`). The pattern is structural: the LLM emits MarkdownV2, the bot strips escapes and re-encodes to HTML, and every time the LLM reaches for a new MarkdownV2 char the strip table needs an update. The architectural alternative is to instruct the LLM to emit Telegram's HTML allowlist directly (`<b>`, `<i>`, `<code>`, `<pre>`, `<a href>`, `<blockquote>`, etc.) and have the bot sanitize-and-send rather than transform. This eliminates the transform layer, ends the patch cycle, and matches Telegram's documented surface. **However**, naïve adoption risks a *worse* failure mode (Telegram rejects malformed HTML with `Bad Request: can't parse entities` → the entire message goes blank, which is strictly worse than today's literal-`\~` visual glitch), so this is a deliberate L-sized story, not a same-day swap. The migration must land prompt change + defensive sanitizer + streaming-path rework + cross-provider compliance + a fallback path simultaneously.

- Acceptance Criteria:
  - [ ] **AC1 — Prompt allowlist instruction.** `backend/api/prompts.py:150-159` (the section that currently instructs MarkdownV2) is replaced with a Telegram-HTML allowlist instruction. Allowlist verbatim (sourced from Telegram Bot API docs, confirmed in David-2's research): `<b>`, `<strong>`, `<i>`, `<em>`, `<u>`, `<ins>`, `<s>`, `<strike>`, `<del>`, `<span class="tg-spoiler">`, `<tg-spoiler>`, `<a href="...">`, `<code>`, `<pre>`, `<pre><code class="language-X">`, `<blockquote>`, `<blockquote expandable>`. Forbid all other tags explicitly — name `<p>`, `<br>`, `<ul>`, `<ol>`, `<li>`, `<table>`, `<h1>`–`<h6>`, `<div>`, `<span>` (other than the spoiler shape) as forbidden. Require `&lt;` / `&gt;` / `&amp;` HTML-entity escaping in literal body text (so inequalities like `if x < 5` and code with `<` survive). Verify: `grep -n "MarkdownV2\|parse_mode='MarkdownV2'\|markdown_v2" backend/api/prompts.py` returns zero matches; new instruction text contains the exact tag list above.
  - [ ] **AC2 — Bot-side HTML sanitizer.** `markdown_to_telegram_html()` in `telegram_bot/handlers/message.py` is replaced wholesale with `sanitize_telegram_html(text: str) -> str` (renamed; old helper deleted, not chained). Behavior: (a) strips any tag NOT on the AC1 allowlist, leaving inner text intact; (b) balances unbalanced tags (an unclosed `<b>` gets a synthetic `</b>` appended, an orphan `</b>` is dropped); (c) validates attributes — `<a>` accepts only `href`, `<pre><code>` accepts only `class="language-X"` where X matches `^[a-zA-Z0-9+_-]{1,32}$`, all other attributes on any tag are stripped; (d) leaves correctly-encoded `&lt;` / `&gt;` / `&amp;` untouched and re-encodes raw `<` / `>` / `&` that aren't part of an allowlisted tag-open. Verify: unit tests in `telegram_bot/tests/test_telegram_html.py` (new file) cover the matrix — non-allowlisted tag (`<p>`, `<ul>`, `<table>`, `<div>`, `<h1>`, `<br>`), unbalanced tags (orphan open/close in both directions), bad attributes (`<a target="_blank">`, `<a onclick="...">`, `<code class="not-a-language!">`), nested-forbidden-inside-allowed (`<b><p>x</p></b>`), and escaped/raw mixed-content (`if &lt;5 and <b>bold</b>`).
  - [ ] **AC3 — Streaming path rework.** [**OPEN QUESTION #1 — Ankit decides; Parminder cannot ready-approve until resolved.**] `telegram_bot/handlers/message.py:625, 670, 725` currently call `edit_message_text(parse_mode='HTML')` on every ~500ms streaming chunk; with HTML-emitting LLM that means partial buffers like `<b>thinking abou` get sent and Telegram rejects every tick → message stays blank until completion. Two options to reconcile:
    - **Option (a) — plain-during-stream, HTML-on-completion.** Streaming `edit_message_text` calls use `parse_mode=None` (raw text shown live, formatting tags visible as text). Final `edit_message_text` after stream completion uses `parse_mode='HTML'` and the sanitized HTML. Simplest. ~50 LOC. Trade-off: user sees raw `<b>` tags in the live stream until completion.
    - **Option (b) — buffer-until-tag-balanced before each edit.** A state machine tracks open tags and only emits to Telegram on safe boundaries (no partial tag at the buffer tail; all tags balanced at emit time). Preserves live formatting. ~150 LOC + careful state-machine tests. Trade-off: code complexity, edge cases around code blocks (`<pre><code>...`) that span many chunks.
    - Whichever option Ankit chooses, the AC has a unit test that simulates a 12-chunk stream emitting `"<b>" + "Hello, " + "Ankit" + "</b>" + " — " + "<i>" + "thinking about your question" + "</i>"` and asserts: (a) no intermediate `edit_message_text` call results in a Telegram-rejection-shaped string, (b) final rendered message is correctly bold+italic.
  - [ ] **AC4 — Catch-and-fallback on parse failure.** Any `edit_message_text` or `send_message` call with `parse_mode='HTML'` is wrapped in a try/except that catches Telegram's `BadRequest` with `"can't parse entities"` (or any 400 referencing parse failure) and automatically retries the same message with `parse_mode=None` and a structured-text degradation (tags stripped via the sanitizer's "extract-text-only" mode, line breaks preserved). Failure path emits a structured WARNING log: `event="telegram_html_parse_fallback"` with conversation_id, the offending message excerpt (first 200 chars), and the BadRequest message. This is belt-and-suspenders for unforeseen LLM output. Verify: integration test where the bot is fed a deliberately-malformed HTML body (e.g., `<b><unclosed>`) — assert (a) the second send call uses `parse_mode=None`, (b) one WARNING log emitted, (c) user receives the text with no exception leaking up.
  - [ ] **AC5 — Cross-provider compliance, Gemini.** Three representative prompts run end-to-end through the live Gemini provider (in dev, via `make logs SERVICE=telegram-bot` smoke): (1) structured list ("List today's calories per item with line breaks"), (2) code snippet ("Show me a Python function that does X"), (3) inequality / math ("Show the inequality if x is less than 5 then ..."). Each Gemini response, run through `sanitize_telegram_html` and sent with `parse_mode='HTML'`, succeeds — no Telegram BadRequest, message visibly correct. Gating: Gemini does NOT produce `<p>`, `<br>`, `<ul>`, `<table>`, `<h1>` in the sanitized output (sanitizer strips them, but the LLM was instructed not to emit them either).
  - [ ] **AC6 — Cross-provider compliance, ChatGPT.** Same three prompts as AC5, run through ChatGPT-5 provider, end-to-end success on Telegram. Same gating.
  - [ ] **AC7 — Cross-provider compliance, Grok-4.** Same three prompts as AC5/AC6, run through Grok-4 provider, end-to-end success on Telegram. Same gating.
  - [ ] **AC8 — Strip-helper retirement.** The 7-line MarkdownV2 strip block from today's hotfix (`telegram_bot/handlers/message.py:236-244`) is **deleted** as part of this story; the entire `markdown_to_telegram_html` helper is removed wholesale and replaced by `sanitize_telegram_html`. Verify: `grep -rn "markdown_to_telegram_html\|MarkdownV2\|markdown_v2" telegram_bot/` returns zero matches in prod code. Document in the PR description that this hotfix is retired so a future engineer doesn't accidentally re-introduce it. (See Open Question #3 for the alternative where the hotfix is kept as a fallback layer.)
  - [ ] **AC9 — Prompt-regression test sweep.** Every test that asserted MarkdownV2-shaped LLM output is updated to assert the new HTML-allowlist behavior. Affected files (David should verify the list at pickup time, but at minimum): `backend/tests/unit/test_prompts.py` (every assertion that probes the prompt-rendering for MarkdownV2 markers), `telegram_bot/tests/test_markdown_html.py` (today's 22-test hotfix file — entire file is replaced by `test_telegram_html.py` per AC2; the 22 hotfix scenarios are re-expressed as HTML-rendering scenarios and must still pass), `telegram_bot/tests/test_message_handler.py` (any handler-level test that asserted MarkdownV2 parse-mode), `backend/tests/integration/test_chat_*.py` (any integration test that assumed MarkdownV2 escape behavior). Verify: full backend + telegram_bot test suite green; total non-decreasing (today's hotfix added 22 tests — replacement file should have ≥22 tests covering the same scenarios in HTML).
  - [ ] **AC10 — Voice / media path consistency check.** `telegram_bot/media_sender.py` does NOT consume `markdown_to_telegram_html` today (per David's hotfix audit). Verify in this story: (a) `media_sender.py` doesn't import the old helper (`grep` zero matches), (b) any caption text that flows through `media_sender.py` and uses `parse_mode='HTML'` is also passed through the new `sanitize_telegram_html` helper. If captions don't use HTML today, no change — document that media captions are plain-text-only.
  - [ ] **AC11 — Telegram 4096-char limit handling.** HTML tag chars count toward Telegram's 4096-char message limit. After `sanitize_telegram_html`, if the output exceeds 4096 chars, the bot splits the message at safe tag boundaries (no split inside an open tag). Verify: unit test feeds a 5000-char input with bolded sections, asserts (a) two send calls, (b) neither half contains an unbalanced tag, (c) total visible content is preserved. (Note: today's bot may already handle the limit for plain text — this AC ensures the splitter is HTML-aware.)
  - [ ] **AC12 — Loki watchdog setup.** Once 25.1 is merged + pushed, Disha (or Murat) sets a calendar reminder for +24h, +7d, +30d to query Loki for `event="telegram_html_parse_fallback"` rate (target: <0.1% of Telegram sends) and zero matches for `Bad Request: can't parse entities` in `telegram-bot` logs. If the fallback rate is >1% at +24h, open Story 25.2 to harden the sanitizer. Calendar reminder set = AC met; reconciliation is followup work.

- Definition of Done (DoD — separate from ACs, signed off before Ankit pushes):
  - All 12 ACs above met and verified.
  - **Murat manual smoke (in dev, uncommitted, via real Telegram chat):**
    1. Structured list: send "List today's calories: 75 for egg, 100 for toast, 200 for coffee" — response renders with line breaks, no literal HTML tags visible, no `Bad Request` in logs.
    2. Code snippet: send "Show me a Python function for fibonacci" — response renders code block correctly (monospace, preserved indentation), no escape leakage.
    3. Math / inequality: send "Explain the rule: if x is less than 5 then double it, else halve it" — response renders the `<` correctly (as `<`, NOT as `&lt;` literally and NOT triggering parse failure).
    4. Multi-paragraph response: long-form answer with multiple bolded headers and bullet-equivalents (using line breaks, since `<ul>` is forbidden) — full message visible, no truncation, no parse failure.
    5. Multi-turn streaming session: 3-turn conversation with live streaming — verify chosen streaming approach (a) or (b) behaves correctly. For (a): live raw text visible during stream, final HTML-formatted at completion. For (b): live formatted text from first token, no flicker, no rejected ticks.
    6. Provider rotation: repeat smoke 1–4 across Gemini, ChatGPT-5, Grok-4 (one prompt each, manually triggered by switching `LLM_PROVIDER` env var or using whatever fallback path is live).
  - **Murat +24h post-push watchdog:** zero `Bad Request: can't parse entities` errors in `telegram-bot` logs (Loki for prod, `make logs SERVICE=telegram-bot` for dev); `telegram_html_parse_fallback` rate <1% (fallback firing means LLM is emitting bad HTML — sanitizer should be catching most of it pre-send, fallback is the last-resort net).
  - **Hotfix retirement confirmed:** the 7-line MarkdownV2 strip block from today's hotfix is gone from `telegram_bot/handlers/message.py`; the 22 hotfix-test scenarios pass under the new architecture (rewritten as HTML-rendering scenarios, same input → correct HTML output).
  - **No regressions on the 22 hotfix scenarios** (the chars `~`, `|`, `>`, `+`, `=`, `{`, `}`, plus the 11 already-handled chars from the original strip table — all 18 MarkdownV2 escape chars from the legacy table render correctly under the new HTML pipeline).

- Edge Cases / Risks:
  - **Mid-stream tag boundary (Option (b) only):** state machine must handle a chunk that ends mid-tag (`<co` waiting for the rest of `<code>`), mid-attribute (`<a href="https://exam`), and mid-entity (`&l` waiting for `t;`). All three patterns need explicit state-machine ACs if Option (b) is chosen — convert this edge case into ACs at ready-approval time.
  - **LLM emits attribute outside allowlist:** `<a target="_blank">`, `<a onclick="...">`, `<a rel="...">`. AC2 sanitizer behavior: strip the disallowed attributes, keep the tag and the allowed attributes. Test case explicit.
  - **Nested forbidden tags inside allowed ones:** e.g., `<b><p>nope</p></b>`, `<i><br></i>`. AC2 sanitizer behavior: strip the inner forbidden tag, keep the outer allowed tag with its inner text. Test case explicit. Document the alternative (fail-safe to plain-text on forbidden-tag detection) and the rationale for choosing strip-and-keep (better UX — user still sees their bolded text, just without the bogus paragraph).
  - **Very long messages:** AC11 covers 4096-char split. Edge case within: a single bolded block longer than 4096 chars (one open `<b>` and a close 5000 chars later). Splitter must close `</b>` at split point in part 1, re-open `<b>` at start of part 2.
  - **Code blocks with literal `<` or `&`:** `<pre><code class="language-python">if x < 5:</code></pre>` — the `<` inside `<code>` must be `&lt;`-escaped. AC1 prompt instruction must be explicit that escaping is required *inside body text including code blocks*. Test case in AC2 covers a code block with raw `<` to verify sanitizer behavior (re-encode raw `<` to `&lt;` if not part of an allowlisted tag-open).
  - **LLM emits Markdown out of habit:** even after the prompt change, providers may emit `**bold**` or `_italic_` or backtick code instead of the requested HTML. The sanitizer should NOT attempt to convert Markdown to HTML — Markdown chars survive as literal text (`**bold**` displays as `**bold**` in Telegram). This is acceptable degradation; if it's frequent, follow-up story 25.X adds Markdown-to-HTML translation. Document this in the PR.
  - **The 22 hotfix-test scenarios:** the literal `\~`, `\|`, etc. inputs from the hotfix tests will not occur post-migration (LLM is no longer emitting MarkdownV2). The replacement test file should cover the *visible-output* scenarios (calorie lists, ranges, comparisons, math) end-to-end, not the MarkdownV2 strip logic.

- Notes:
  - **Why a new Epic 25, not under Epic 24.** Epic 24 is "Provider Modernization & Tech Debt" — its theme is LLM provider SDK migration (the `google.genai` swap, future thinking-budget tuning, batch API for proactive worker, etc.). Story 25.1 is bot-side rendering — different layer (Telegram bot, not backend providers), different failure mode (Telegram parse rejection vs SDK breakage), different test surface (`telegram_bot/tests/`, not `backend/tests/providers/`). Forcing it under E24 dilutes that epic's theme. Today's hotfix being the *third* patch on the same helper is the trigger — that's a code smell saying the layer needs its own theme. Future stories under E25 absorb other Telegram-bot reliability work (rate-limit handling, Telegram API flapping, edit-message race conditions, voice/media-sender consistency).
  - **Why this is L, not M.** The prompt change is ~10 LOC. The sanitizer is ~150 LOC of careful HTML-parse logic (well-known hard problem if done from scratch — recommend evaluating whether `html.parser` from stdlib or a small dep like `bleach` is appropriate, but Parminder approves dep at ready-approval time). The streaming refactor is the wild card — 50 LOC for Option (a), 150+ LOC for Option (b). Cross-provider testing requires three live provider runs. Test sweep touches multiple files. David-2 said 2 days realistic; concur.
  - **Sequencing rationale.** The hotfix on `develop` MUST be pushed first — production is currently visibly broken for users typing calorie lists, math, etc. The hotfix is the safety net while 25.1 is in flight. Story 24.1 (`google.genai`) ideally lands first because the new SDK may behave differently in response to the new prompt instruction; if 24.1 is delayed >1 week, 25.1 can proceed against current SDK with a re-validation gate when 24.1 lands.
  - **Cost-of-not-doing rationale.** This is a P1, not P0, because the hotfix has already restored visible correctness in production (post-push). The architectural pivot is the *right* fix but not the *urgent* fix. P1 says: do it deliberately, not now. If a fourth patch becomes necessary on the strip-helper, escalate to P0.
  - **Architecture trade-offs already considered:**
    - *Stay on MarkdownV2, expand strip table to all 18 chars (already done in hotfix).* Trade-off: keeps the patch cycle alive. Every new MarkdownV2 char the LLM reaches for is another patch. Rejected.
    - *Switch to plain-text only (no formatting at all).* Trade-off: regresses UX — users lose bold/italic/code formatting which is genuinely useful for calorie lists, code snippets, etc. Rejected.
    - *LLM-emit-HTML-direct (this story).* Trade-off: 2-day investment + a worse failure mode if shipped half-baked. Selected, gated on doing it correctly with sanitizer + fallback + cross-provider testing.

- Open Questions (resolve at ready-approval):
  - **OQ1 — Streaming approach (a) plain-during-stream OR (b) buffer-until-tag-balanced.** Ankit's call. Dominates the bottom of the size estimate (50 vs 150+ LOC). Parminder cannot ready-approve until this is resolved because AC3 has two divergent test surfaces. Default if Ankit defers: **(a) plain-during-stream** — simplest, fewest edge cases, lowest risk. Live formatting during stream is a nice-to-have, not a must-have; users already accept ~500ms chunk latency, so an extra "wait for completion to see formatting" is a small UX cost.
  - **OQ2 — Epic placement.** Drafted as new Epic 25 (Telegram Rendering & Bot Reliability). Alternative: under Epic 24 (Provider Modernization & Tech Debt) with a broader interpretation of "tech debt." Disha's recommendation: Epic 25, because the layer is different (bot vs backend providers) and there are likely 3–5 future bot-reliability stories that will want to land under the same theme. Fenny / Ankit confirmation requested.
  - **OQ3 — Hotfix retention strategy.** Two options: (a) **Full retirement** — delete the strip-helper + 22 hotfix tests; the new HTML-direct pipeline replaces it (AC8 as written). (b) **Keep as fallback layer** — sanitizer first, but if for any reason an LLM emits MarkdownV2 (e.g., prompt-cache hit serving stale instructions, model regression), the strip-helper catches it. Trade-off: (a) cleaner code, retires the patch cycle definitively; (b) more defensive but keeps a known-fragile helper alive. Disha's recommendation: **(a) full retirement** — the AC4 catch-and-fallback (parse-rejection → plain-text retry) is the correct safety net; keeping the strip-helper is over-defending. Parminder's call at ready-approval.

### Bug 25.2: Gemini cost-math double-counts cached tokens (5.5x overstatement on cache-hit traces)
- Status: done (Murat APPROVED cycle 1 of 3 on 2026-04-25 — independent test re-run reproduced 746 passed / 14 skipped / 0 failed, 8 Bug 25.2 tests green with high mock fidelity (SDK-agnostic doubles); AC5 37-trace re-audit on existing Langfuse data reproduced Parminder's 5.539x overstatement on `1cd04de7` exactly + identified 15 cache-hit traces with per-trace overstatement ranging 1.675x-6.550x and 1.515x aggregate (33.98% projected post-fix reduction); AC4 ±2% recon runbook documented for Ankit's post-push gate; AC7 +7d watchdog unchanged from 24.1 + +30d watchdog threshold recalibrated from ≥40% to **≥50%** based on empirical 24h projection (theoretical ceiling ~63% with full cache adoption, leaves headroom for noise); AC2 Harpreet nit confirmed (chatgpt_provider:664 success-path lacks mirror comment) — defer to follow-up cleanup, NOT 25.2 blocker; production logs clean (zero ERROR/CRITICAL past 1h, zero `cached_token_count_anomaly` matches); zero live API calls; ready for Ankit's push)
- Type: Bug (not Story) — pre-existing defect in cost-emission shape, surfaced by 24.1's AC15 plumbing
- Assigned: Parminder (for ready-approval); David (for impl post-approval)
- Priority: P1
- Size: S (~5 LOC across 3 sites in `gemini_provider.py` + symmetry check in `chatgpt_provider.py` + 1 invariant unit test)
- Created: 2026-04-25
- Discovered: 2026-04-25 by Murat (during 24.1 testing) and independently confirmed by Parminder (37-trace Langfuse audit, post-24.1)
- Dependencies:
  - **HARD**: Story 24.1 must be PUSHED to production first. The AC15 cached-token plumbing in 24.1 is what exposed this defect (cached_tokens started flowing into the cost-emission code path). Don't bundle 24.1 + 25.2 in the same uncommitted wave — 24.1 is verified-shippable AS-IS; 25.2 is the follow-on fix.
  - **SOFT**: none. The fix is isolated to provider emission shape; no cross-cutting concerns.
- Blocks: none. The bug is observability-correctness only — Annie's accounting overstates cost on cache hits, but Google bills correctly regardless. Per Parminder's worked example (trace `1cd04de7`): Google charges $0.008932; Annie books $0.049472; user is not overcharged, just mis-recorded.
- Review Cycles: 0
- Defect Summary: `backend/api/observability/cost.py:94-160` adds `cached_cost` ON TOP of the full-priced `input_cost` rather than substituting the discount price for the cached portion. Pre-existing March 2026 commit; NOT introduced by 24.1 — but 24.1's AC15 plumbing exposed it because that's when `cached_tokens > 0` started flowing through the emission path. **Hardcoded rates in `cost.py` are CORRECT** ($2 / $12 / $0.20 per 1M input/output/cached, ≤200K tier — matches Google's published gemini-3.1-pro-preview pricing). The defect is in the EMISSION SHAPE: Annie emits `input = prompt_tokens` (full prompt at full rate) AND `cached_input = cached_tokens` (cached portion at discount rate) ON TOP, but Google's `prompt_token_count` ALREADY INCLUDES `cached_content_token_count` (cached are a subset of input, not additive). Result: every cached token gets billed in Annie's books at **$2.20/M instead of $0.20/M** (full input rate + cached rate stacked). Direction reversed from pre-23.1: pre-23.1 we UNDERSTATED 8x (chunk-count substitution); post-23.1 + 24.1 we OVERSTATE 5.5x on cache hits.

- Acceptance Criteria:
  - [ ] **AC1 — Gemini provider emission fix.** `backend/api/providers/gemini_provider.py` updated so the Langfuse `usage["input"]` field is **non-cached input only** (i.e., emits `prompt_tokens - cached_tokens`) at all three emission sites. **Parminder's preferred shape (helper-level fix):** modify `_extract_usage_metadata` (line 338) to return a `non_cached_input_tokens` key alongside `prompt_tokens` and `cached_tokens`, then reference that key at the three emission sites — single source of truth, all sites benefit automatically. Backwards-compat: helper has exactly ONE caller today (`gemini_provider.py:760`), so changing the return shape is safe. Acceptable alternative: subtract at each emission site (David's call if helper-shape change feels invasive). Verified line numbers post-24.1 push: (a) lines 1044-1053 (success path, `end_kwargs["usage"]` block — `"input": prompt_tokens` at L1045); (b) lines 1428-1437 (ended-without-STOP path — `"input": prompt_tokens` at L1429); (c) lines 486-495 (`_trace_error_generation` — `"input": last_usage["prompt_tokens"]` at L487). Verification gate: a final grep for `"input": prompt_tokens` and `"input": last_usage\["prompt_tokens"\]` returns zero matches; the three emission sites all reference the non-cached value (with `max(0, ...)` guard per AC8). The `cost_info` call to `calculate_cost(...)` continues to receive raw `prompt_tokens` and `cached_tokens` separately (cost.py keeps its current signature; the fix is at the *Langfuse emission* layer, not in cost.py — cost.py's per-1M math is correct, the bug was Annie sending Langfuse a mis-shaped `usage` dict that resolved to full-rate billing on the cached subset).
  - [ ] **AC2 — ChatGPT provider symmetry: documented no-op (Parminder verified 2026-04-25).** Inspection of `backend/api/providers/chatgpt_provider.py` confirms the symmetric defect does NOT exist: the ChatGPT path never reads `prompt_tokens_details.cached_tokens` from OpenAI's `usage` block, never passes a `cached_tokens` value to `calculate_llm_cost`, and never emits `input_cached` / `cached_cost` to Langfuse (success path L656-663, error path L173-180). The bug requires a non-zero cached value flowing into emission, which doesn't happen here. **No code change in this bug.** Parminder note: OpenAI's API does silently cache prompts ≥1024 tokens at a 50% discount, and Annie is currently NOT capturing that discount — but that's an *additive plumbing story* (carve under a future provider-cost epic), not a *fix-double-count* bug. Out of scope for 25.2. PR description should explicitly call out: "ChatGPT no-op: no cached-token plumbing exists today, so no double-count to fix; OpenAI auto-cache discount capture is deferred to a separate story."
  - [ ] **AC3 — Invariant unit test (mock-only, no live API).** New unit test in `backend/tests/unit/providers/test_gemini_cost_emission.py` (or extend an existing test file at Parminder's discretion) that asserts the invariant: for any generation where `cached_tokens > 0`, the emitted `usage` dict satisfies `usage["input"] + usage["cached_input"] == prompt_token_count`. Use mocked `usage_metadata` shapes — no live Gemini API calls. Concrete regression-test data point: trace fixture `1cd04de7` (`prompt_token_count=22463, completion_token_count=41, cached_content_token_count=20270`) — assert emitted `usage["input"]=2193, usage["cached_input"]=20270, usage["output"]=41`, and assert `2193 + 20270 == 22463`. Cover all three emission sites (success path, ended-without-STOP path, `_trace_error_generation`).
  - [ ] **AC4 — End-to-end recalculation reconciles within ±2%.** After fix lands, `total_cost = input_emit × $2/M + cached_emit × $0.20/M + output × $12/M` should match Google's billing within ±2% for any cache-hit trace. Verify on at least 5 post-fix cache-hit traces in Langfuse. For trace fixture `1cd04de7`: post-fix Annie should book $0.008932 (matching Google's actual bill); pre-fix Annie booked $0.049472 (5.5x over). Documented in bug closure with the 5 trace IDs and their reconciliation deltas.
  - [ ] **AC5 — Variance reconciliation on the 37-trace audit.** Re-run Parminder's 37-trace Langfuse audit (existing data, no new traffic — `get_generations` paged over the same 24h window already captured in bus 2026-04-25 14:25). Variance shifts from ~+5.5x overstated on cache-hits to ~±2% reconciled. Document the before/after per-trace variance in the bug closure note.
  - [ ] **AC6 — No regression in no-cache cost.** Existing no-cache observations (per Parminder's audit: f5934bca, ad684dcf, 84006382, 8d54f54f, etc. — they currently reconcile to the cent) continue to reconcile to the cent post-fix. The arithmetic `prompt_tokens - cached_tokens` when `cached_tokens=0` produces `prompt_tokens`, so no behavioral change is expected — this AC just verifies the expected no-op holds.
  - [ ] **AC7 — Watchdog ownership: 25.2 supersedes 24.1's +7d/+30d cost-reduction watchdog (Parminder resolved 2026-04-25).** 24.1's +30d ≥40% input-cost-reduction threshold was calibrated against the over-stated post-24.1 baseline (full-rate prompt + additive cached_cost), so the comparison is apples-to-oranges with real Google billing. Bug 25.2 ownership: (a) at fix-merge time, Disha (or Murat) sets a fresh +7d and +30d watchdog using post-25.2 Annie-booked numbers as the baseline AND cross-references AI Studio billing for ground truth (the 25.2 watchdog runbook becomes canonical); (b) 24.1's watchdog runbook in the 2026-04-25 bus entry stays as historical record but is annotated "superseded by 25.2 watchdog at merge"; (c) 24.1's separate +24h watchdog (deprecation-warning gone, cached_tokens plumbing fires, no SDK regressions) is unaffected and stays with 24.1. Murat owns the runbook write-up at fix-merge time.
  - [ ] **AC8 — Defensive guards on subtraction edge cases.** The expression `prompt_tokens - cached_tokens` is wrapped as `max(0, prompt_tokens - cached_tokens)` to guard against the unlikely case where a math glitch (or upstream SDK bug) produces `cached_tokens > prompt_tokens`. If the guard fires, emit a structured WARNING log: `event="cached_token_count_anomaly"` with `prompt_tokens`, `cached_tokens`, `conversation_id`. This is belt-and-suspenders; it should never fire in practice.

- Definition of Done (DoD — separate from ACs, signed off before push):
  - All 8 ACs above met and verified.
  - Backend test count non-decreasing (738 baseline post-24.1, per Murat's count). The new invariant test (AC3) adds ≥1 test; total should be 739+ after this bug lands.
  - **No live-API smoke required.** The fix is in the emission shape, not call behavior. Existing Langfuse traces + mock unit tests fully cover the change. (Per Ankit's cost-conscious testing rule — cache-hit reconciliation is verifiable from already-recorded Langfuse data; no need to spend tokens to validate accounting math.)
  - **Manual reconciliation gate (Ankit's signoff):** Ankit picks one recent cache-hit trace post-merge and compares Annie's booked cost to AI Studio billing. <2% variance = signoff. (Per Parminder's open question in bus 14:25 — this is the same end-to-end confirmation she requested.)

- Edge Cases:
  - **First-turn generation, `cached_tokens = 0`:** the `prompt_tokens - cached_tokens` arithmetic produces `prompt_tokens`, so no behavioral change for non-cached turns. Invariant test (AC3) should explicitly cover the `cached_tokens=0` case to lock this no-op behavior.
  - **`usage_metadata.cached_content_token_count` absent on a chunk:** older SDK shapes or empty/partial responses may not include this field. David's `_extract_usage_metadata` already defaults `cached_tokens=0` in this case (verified during 24.1 implementation), so the subtraction is safe. Test coverage: a mocked `usage_metadata` with NO `cached_content_token_count` key — assert `usage["input"] == prompt_tokens` and `usage["cached_input"] == 0`.
  - **`cached_tokens > prompt_tokens` (math anomaly):** AC8's `max(0, ...)` guard handles this. Should never happen — Google's contract guarantees `cached ⊆ prompt` — but defensive code is cheap. Test coverage: a mocked anomaly shape (`prompt_token_count=100, cached_content_token_count=120`) — assert no negative emission and a WARNING log is emitted.
  - **ChatGPT provider has no cached-token plumbing yet:** if AC2 inspection finds ChatGPT doesn't emit `cached_input` at all today, fix is a no-op for ChatGPT. Document that ChatGPT cached-token correctness is out of scope for this bug and will be addressed if/when ChatGPT cached-token plumbing is added.
  - **Pre-existing Langfuse traces remain over-stated:** historical traces booked pre-fix retain their over-stated values in Langfuse — this bug does NOT retroactively correct historical data. Cost dashboards / 30d baselines that include pre-fix traces will continue to show inflated numbers until the over-stated traces age out of the rolling window. AC7 watchdog acknowledges this.

- Notes:
  - **Epic-placement caveat (Ankit's awareness).** Ankit named the slot "25.2", placing this under Epic 25 (Telegram Rendering & Bot Reliability). However, this bug is a Gemini provider cost-math fix — different domain (backend providers / observability, not Telegram bot rendering), different failure mode (over-stated accounting, not Telegram parse rejection), different test surface (`backend/tests/providers/`, not `telegram_bot/tests/`). Two paths forward — **defer the decision until at least one more provider-cost bug surfaces**:
    - (a) Accept the misnomer for now and rename Epic 25 to something broader like "Bug & Tech Debt Cleanup" if more cross-domain bugs accumulate under E25.
    - (b) Carve a new "Epic 26: Provider Cost & Observability Hardening" if 24.x-style cost stories continue (likely — Parminder has flagged ChatGPT cached-token plumbing as a future scope item, plus the AC7 watchdog will likely surface follow-ons).
    - Recommend: keep as 25.2 for now per Ankit's directive; revisit at next sprint planning if Epic 25 grows beyond Telegram concerns.
  - **Sequencing rationale.** Bug 25.2 lands AFTER Story 24.1 is pushed. Don't draft both in the same uncommitted bundle — 24.1 is verified-shippable AS-IS (Murat approved cycle 1), and bundling the cost-math fix risks delaying the SDK migration. Push 24.1, then immediately stack 25.2 on top.
  - **Concrete reproduction fixture.** Trace `1cd04de7` (prompt=22463, output=41, cached=20270): Annie books $0.049472; Google actually bills $0.008932 (per Parminder's worked example, bus 14:25). Use this as the regression-test data point in AC3 — locks the bug shape in a unit test for future safety.
  - **No code, no tests run by Disha.** This is the product-spec only. Parminder reviews → approves "ready"; David implements; Murat verifies via Langfuse trace reconciliation (no live API traffic needed).
  - **Fix size estimate (Parminder's analysis).** ~5 LOC of behavior change: 3 sites in `gemini_provider.py` (2-LOC change each: subtract cached_tokens, add `max(0, ...)` guard), plus AC2 inspection of `chatgpt_provider.py` confirmed no-op. Plus 1 unit test file (~30-50 LOC). Total: small, contained, low-risk.
  - **Architectural shape steer for David (Parminder, 2026-04-25).** Helper-level fix is preferred: extend `_extract_usage_metadata` (line 338) to return `non_cached_input_tokens` alongside the existing `prompt_tokens` / `cached_tokens` / `completion_tokens` keys; the three emission sites then reference the new key. Single source of truth, no duplicated `max(0, ...)` guard, no risk of one site being missed in future edits. **Backwards-compat verified safe**: `_extract_usage_metadata` has exactly ONE caller (`gemini_provider.py:760`); changing the return-dict shape is a single-call-site refactor. The cost-calc path (`calculate_cost(...)`) continues to receive raw `prompt_tokens` and `cached_tokens` separately — `cost.py` keeps its current signature; the bug is *only* in what Annie sends to Langfuse as `usage["input"]`. If David finds the helper-shape change feels invasive at implementation time (e.g., it ripples into the existing 24.1 unit tests for `_extract_usage_metadata`), the fallback is subtract-at-each-emission-site. Either shape satisfies AC1 — Parminder's preference is the helper level.
  - **Error-path partial usage (Parminder, 2026-04-25).** `_trace_error_generation` (L437-501) only emits the `usage` block when `last_usage is not None` and contains `prompt_tokens` + `completion_tokens` (gated by `_extract_usage_metadata` returning non-None — both required fields present). `cached_tokens` defaults to 0 when `usage_metadata` lacks `cached_content_token_count`. So the partial-usage error path does NOT need a special branch: the helper-level fix (`non_cached = prompt - cached`) is well-defined for `cached=0` (no-op, returns prompt) and the `max(0, ...)` guard handles any anomaly. No additional defensive code needed beyond AC8.
  - **Historical recalculation deliberately out of scope (Parminder, 2026-04-25).** Pre-25.2 cache-hit traces in Langfuse remain over-stated by 5.5x. Story 23.1's `cost_reconciliation.py` is NOT being updated with a "before/after" awareness flag in this bug — the +30d cumulative number simply gets dirtier until pre-fix traces age out of the rolling window (covered explicitly in the Edge Cases section above). Rationale: the historical noise self-resolves over 30 days; adding a fix-date awareness flag is more code than it's worth for a transient correctness gap. AC7's watchdog runbook will document the "pre-fix vs post-fix baseline" boundary so dashboards aren't misread during the transition.

- Open Questions (resolved by Parminder at ready-approval 2026-04-25):
  - **OQ1 — ChatGPT scope: RESOLVED — keep Gemini-focused (AC2 = documented no-op).** Confirmed by code inspection: ChatGPT provider has zero cached-token plumbing today (no read of `prompt_tokens_details.cached_tokens`, no `cached_tokens` arg to `calculate_llm_cost`, no `input_cached`/`cached_cost` in Langfuse emission). The symmetric double-count defect cannot exist where the cached value never flows. Adding OpenAI auto-cache discount capture is *new plumbing*, not a *bug fix*, and gets carved as a separate story (likely under a future provider-cost epic alongside Anthropic/Grok cache work).
  - **OQ2 — Watchdog ownership: RESOLVED — 25.2 supersedes 24.1's +7d/+30d cost-reduction watchdog.** 24.1's runbook stays as historical record (annotated "superseded by 25.2"); 25.2's runbook becomes canonical for cache-discount thresholds and is cross-referenced against AI Studio billing for ground truth. 24.1's separate +24h watchdog (deprecation gone, cached_tokens plumbing fires) is unaffected and stays with 24.1.

## Backlog
Epic 22 in flight (22.1/22.2/22.3 done; 22.2.1/22.2.2 done; 22.4 reshaped + deferred; 22.5 deferred). Epic 23 DONE (23.1 pushed 2026-04-25). Epic 24 in progress 2026-04-25 — Story 24.1 APPROVED by Murat cycle 1, moved to done; awaiting Ankit's commit + push. Epic 25 opened 2026-04-25 — Story 25.1 drafted (P1, Telegram-HTML pivot), awaiting Parminder ready-approval pending Ankit's streaming-approach decision; **Bug 25.2 drafted (P1, S, Gemini cost-math fix), awaiting Parminder ready-approval; sequenced after 24.1 push**.

## Hotfixes / Inflight
- 2026-04-25 — `markdown_to_telegram_html` MarkdownV2 strip block was missing 7 of 18 escape chars (`\~ \| \> \+ \= \{ \}`); literal backslashes leaking into Telegram HTML rendering. P0 user-facing visual. Fix applied (uncommitted on develop) by David: `telegram_bot/handlers/message.py:236-244` (+9 lines including `\>` → `\&gt;` post-html.escape note); new test file `telegram_bot/tests/test_markdown_html.py` (22 tests, full 18-char set + combined cases). Suite: 138 passed (116 prior + 22 new). Awaiting Ankit verification + push, or Murat smoke. NOT a Story (single-helper; ceremony skipped per David triage). **Architectural follow-up filed as Story 25.1 (P1, L) — LLM-emit-Telegram-HTML-direct pivot.** Hotfix retirement is part of 25.1's AC8/DoD; hotfix must be PUSHED first as 25.1's hard dependency.
- 2026-04-25 — Telegram chat-action UX band-aid for post-24.1 TTFT regression (~9s → ~22s). "Annie is thinking..." dominated the visible wait while "Composing response..." flashed by alongside the first token because the SSE status_queue was only drained INSIDE the `async for event in stream_chat_completion(...)` loop body, so the queued "Composing..." status sat behind the LLM's first event. Fix applied (uncommitted on develop) by David: backend eager-drains the status_queue **before** awaiting the first LLM event. `backend/api/routes/stream.py` at three sites — Gemini path (line ~552), OpenAI/Grok path (line ~865), max-iterations fallback (line ~1283); +24 LOC total (8 lines per site, idempotent drain block matching existing pattern). New tests: `backend/tests/unit/test_story_11_5_status_phases.py::TestComposingEagerDrain` (2 tests gating mock LLM stream behind `asyncio.Event` to prove status frame yielded before first LLM event in both Gemini and Grok paths). Suite: 519 backend unit passed (+2 new); 138 telegram-bot unchanged. Awaiting Ankit verification in real Telegram + push. Orthogonal to (and does NOT fix) the underlying TTFT regression — separate investigation.

## Session Note
Agent tool unavailable this session (confirmed via ToolSearch — same constraint as bus 14:25). Fenny is running waves 1-4 as Fenny-as-proxy with clearly labeled role hats per change.
