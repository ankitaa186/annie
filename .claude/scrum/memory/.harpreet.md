# Harpreet's Project Understanding (Code Reviewer)
Last Updated: 2026-04-16 10:00

## Project Overview
Annie - personal AI companion chatbot. Microservices architecture with FastAPI backend, MCP server, Telegram bot, web UI. Production-running system.

## Code Quality Assessment

### Strengths
- Clean provider abstraction for LLMs (base.py + implementations)
- Modular MCP tool registry
- Structured logging with sensitive data masking
- Middleware layer for auth and rate limiting
- Pydantic models for request/response validation
- Docker health checks on all services

### Concerns to Watch
- Test coverage is incomplete (Epic 6 not done)
- 272 commits by mostly one developer - may have accumulated patterns that need review
- `media_sender.py` is 38KB - likely needs refactoring
- `backend_client.py` is 22KB - may be doing too much
- `file_handler.py` is 26KB - large file, complexity risk
- `mcp_server/server.py` is 14KB - check if it's well-structured
- No CI test gates visible (tests don't block merges?)

### Security Posture
- API keys masked in logs (good)
- Cloudflare auth middleware (good)
- Rate limiting implemented (good)
- File handling: process-and-discard, no persistence (good)
- Home Assistant control restricted to allowlist (good)
- Terminal tool restricted to admin user (good)
- `.env` in .gitignore (good)

### Coding Standards
- Python 3.12+ with type hints (verify consistency)
- FastAPI with async patterns
- Redis for all ephemeral state
- Structured JSON logging in production
- Environment-based configuration

### Review Focus Areas
- Large files (media_sender, backend_client, file_handler) - check for SRP violations
- Error handling consistency across providers
- Async/await correctness
- Redis connection management (verify proper cleanup)
- Test quality when tests exist

## Standing Directives
- **Source of Truth**: The codebase and git log are the ONLY source of truth. The BMAD planning docs in `docs/` (PRDs, architecture plans, epic files, sprint plans) may be outdated and MUST NOT be treated as authoritative. When reviewing code, verify correctness against the actual codebase and `git log` / `git diff`, not the planning documents.
