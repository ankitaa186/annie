# Scrum Bus -- 2026-04-17

## [session] Fenny -> team: [TASK] Conversation quality audit — 4 agents in parallel
Fenny: Ankit wants the team to evaluate the QUALITY of Annie's real conversations — not the code, the conversations themselves. Deploy 4 agents in parallel, each with a distinct lens, each pulling REAL data (not hypothesizing from code):

- Disha (PM): Are Ankit's actual needs being met? Is decision-support value being delivered? Where is Annie useful vs just verbose?
- Parminder (Architect): LLM + tool-call patterns, prompt-handling quality, context bleed, error modes, cost shape.
- Harpreet (Reviewer): Response quality — hallucinations, tone, safety, memory accuracy, tool-call correctness.
- Murat (Tester): Where does Annie fail — flow gaps, edge cases, regressions, recurring failure patterns.

Data sources live:
- Langfuse (us.cloud.langfuse.com) — full traces w/ session_id = conversation_id, user_id = telegram_1169491032, release = annie-dev
- agentic-memories (localhost:8080) — stored memories, decisions, sentiment, preferences
- Redis (annie-redis container) — live session state, conversation:{id}:history, daily_context, summaries
- Docker logs — backend + telegram-bot + mcp-server (last 7 days)

Agent tool unavailable (ToolSearch confirms — same as prior sessions this sprint). Running as Fenny-as-proxy with role hats per analysis. Data pull happening in parallel now.

---

## [session] Fenny -> team: [STATUS] Data pull complete, 4 parallel analyses ready
Fenny: Pulled from Redis (conversation:conv_b10a68a0829b47d2, 65 msgs / 62KB / 5h span / 16 user turns), daily_context:1169491032:2026-04-16 (scratchpad has meals + workout + testing_status), profile:1169491032 (85% complete — rich), conversation summary (generated 06:55 UTC), and backend/mcp/telegram-bot logs (last 48h). Ran gap analysis, tool-call user_id audit, response-length stats. Four role analyses follow.

---

## [session] Disha -> Fenny: [STATUS] Conversation quality — Product lens
Disha (proxied by Fenny): Reviewed the full 5-hour 16-user-turn conversation from Ankit's 2026-04-17 session.

**The product is delivering real value — with specific friction points.**

**What's working (from Ankit's actual usage):**
- Calorie tracking + body-recomp coaching is the killer loop. Ankit logged 3 separate meal increments (dinner → biscuit/ginseng → protein shake) across a 2-hour window; Annie computed running totals each time ("1,470-1,500" → "1,520-1,550" → "1,680-1,710") and proactively called out deficit size vs his actual TDEE (~2,300-2,400).
- Decision-support hit: when Ankit asked "is this Premier Protein good?", Annie did what a good advisor does — confirmed the macros, flagged the caffeine-at-night risk against his known 8:30pm meeting schedule. That's personalized reasoning, not generic info.
- Profile context actively used: Annie pulled age, weight, BMR, muscle mass, Intuit meeting schedule into decisions. That's the investment in the 85%-complete profile paying off.
- Honest "I don't know" on the NYC-bar memory miss. Instead of hallucinating, Annie said "I don't have that saved" and offered hints — exactly the behavior you'd want from a trusted advisor.
- Conversational warmth and persona is consistent ("tank fuel" / "structural steel" metaphors). Feels like a specific character, not a generic LLM. That's brand.

**What's hurting decision-support value:**
1. **Context horizon gap is the #1 complaint surface.** Three user turns landed after 80-96 minute silences (msg 29 "which bar in NYC", msg 43 "rooftop high floor", msg 61 "test store memory"). In two of those, the scratchpad's meals/workout context was still rendered so Annie stayed oriented. But for things NOT in the scratchpad (NYC bar recommendation from a prior conversation, the geographic location when suggesting Fleur Room), Annie lost the thread. Epic 22 is the right bet.
2. **Location-awareness fail at msg 54.** Annie fully helped Ankit identify "The Fleur Room" in NYC, then enthusiastically asked "Are you thinking about heading back there soon?" — despite Ankit literally living in Newark, CA (in the profile she just loaded 35 messages ago). Ankit had to correct her: "No I am in newark,ca remember". This is a trust-eroding moment — the AI doesn't maintain situational model.
3. **Over-responds on acknowledgments.** Avg assistant response: 979 chars. Avg user message: 37 chars. **26x verbosity ratio.** On "I just had it" (13 chars), Annie replied with 8 paragraphs about caffeine + sleep advice (msg 28). That's coach-stack running on a simple log entry. Room to adapt response length to input intent.
4. **Outcome loop still missing.** Annie recommended "aim for 1,900-2,000 cal instead of 1,500" at msg 14. At msg 17 (90 min later), Ankit added a biscuit + ginseng (~50 cal more, not 400-500). Annie didn't flag the gap between the recommendation and the behavior. No mechanism for "yesterday I said X, today you did Y, let's close the loop." This is the Jarvis^100 gap your memory already flags.
5. **Store_memory is still under-used.** Annie stored one memory on "user drinks Premier Protein Shake" (msg 22) — reasonable. But the "user lives in Newark, CA" correction at msg 55 went into the profile (via msg 56 profile read) but there's no evidence Annie made the connection in long-term storage. The NYC bar preference DID get stored (msg 52). So store_memory is called, but inconsistently — permanent facts vs session-only is still fuzzy for the LLM despite the 22.3 prompt rewrite.
6. **Sole-user feedback loop**: Ankit typed "test store memory" (msg 61). He's still QA-ing the tool himself because he doesn't fully trust it. That's a product signal.

**User need: being met? Mostly yes, specifically:**
- Daily nutrition/fitness tracking: YES (with Epic 22 scratchpad fully closing the loop)
- Personalized advice grounded in profile: YES, when profile is loaded
- Long-term memory recall: PARTIAL (semantic search missed the NYC bar; Annie fell back to web_search + user hints)
- Decision-support momentum: STRONG for single-turn advice, WEAK for cross-day / cross-session follow-through (the outcome loop)

**Top product asks (ranked by value):**
1. Ship Epic 22 (22.1/22.2/22.2.1/22.2.2) to main — it's in uncommitted develop and directly fixes #1 + #2 above
2. Response-length adaptation: when user message is <30 chars AND not a question, cap response at 2 paragraphs
3. Build the outcome loop (Epic-new): store the RECOMMENDATION at turn N, reference it at turn N+1 or tomorrow, close the loop
4. Location-check tripwire: if user geography is in profile AND assistant is about to suggest a physical action, inject "user is at {location}" into reasoning

Handoff to Fenny.

---

## [session] Parminder -> Fenny: [STATUS] Conversation quality — Architecture lens
Parminder (proxied by Fenny): System behavior analysis from the same 65-msg trace + logs.

**THE HEADLINE FINDING: LLM-provided user_id is wrong on 16 of 18 tool calls (89%) in the 5-hour conversation.**
- Real user_id: `1169491032`
- What Gemini 3 Pro emitted in tool arguments: `121325608`, `121111664`, `1326229449`, `1322144573`, `[system_user_id]` (literal placeholder), `12345`, `107612140` — all garbage.
- **A backend guardrail (`backend/api/mcp_client.py`?) detects this and silently overrides.** Log evidence:
  `WARNING: Overriding LLM-provided user_id with correct value [original_user_id=107612140, correct_user_id=1169491032]`
- Only 2 of 18 calls used the correct ID (both `update_daily_context` on msg 1 and msg 7, early in the session). All later calls hallucinated. Pattern: the longer the context, the worse Gemini gets at remembering the user_id string. This is a context-attention failure, not a prompt-injection failure — the ID is in the system prompt and profile tool responses.
- Cost: zero user-visible failures (the override is belt-and-braces and works) but a LOT of wasted warning surface, and it papers over what would otherwise be a data-integrity bomb if the override ever regresses. The system is one removed guardrail from catastrophic cross-user contamination. **Strengthen-the-guardrail value is high; making the LLM stop emitting garbage is the right long-term fix.**

**Context handling:**
- Context at msg 42 was 11,749 tokens with message_count=42. Well under the 20k overflow threshold.
- This means story 22.1 (always-inject rolling summary regardless of token budget) would have fired if it were merged. It's not (still uncommitted on develop). The conversation hasn't hit the overflow path, so the summary never gets injected, so cross-gap recall relies only on in-context messages + scratchpad. Explains why msg 54 lost Ankit's location: the profile response from msg 41 was 13 messages ago, flooded by web_search results and long assistant responses.
- Scratchpad (daily_context) IS working: meals/workout written 4 times total, visible in 2026-04-16 Redis key. This is why msg 4 ("Total calories?") and msg 28 (after 90-min gap) both retained the meal list. The scratchpad is doing its job.
- Summary cache `conversation:conv_b10a68a0829b47d2:summary` WAS generated (at 06:55 UTC, message_count_at_generation=36) — good. But without 22.1, it's only injected on overflow, which didn't happen.

**Tool-call patterns:**
- 18 tool calls over 16 user turns — healthy ratio.
- `retrieve_memories` called 6 times; 5 returned "Project Peon Construction" (a hardware project memory) for queries about NYC bars. **Semantic memory is returning top-K wrong-topic results.** agentic-memories ranking is not producing useful hits for this query shape — consistent with what Annie said honestly: "semantic memory couldn't locate it." Either the memory wasn't stored in the first place, or the embedding model is matching on wrong signal (e.g., "NYC" tokens).
- `web_search` called twice on msg 46 + 48. Both with tavily. Returned lists of NYC rooftop bars. Sensible escalation path when memory missed.
- `update_daily_context` called 4 times, ALL with key=meals or key=workout. Scratchpad used as designed (meals overwrites, workout set once).
- `store_memory` called 3 times: once for "drinks Premier Protein", once for "Fleur Room rooftop preference", once for the explicit test. Usage is pretty conservative — which was Ankit's concern before 22.3. Post-22.3 looks measured, not over-eager. Good.
- `get_user_profile` called 3 times. Redundant on calls 40 and 56 — profile was already loaded at 11. LLM not remembering it saw the profile. Related to the user_id hallucination — same attention drift.

**Provider behavior:**
- Active provider this session: **gemini-3-pro-preview** (temp=1.0, max_output_tokens=16384, max_tool_iterations=20, safety_setting=BLOCK_NONE). That's the preview model — may not be the production recommendation.
- Zero errors in 48h of backend logs. Zero telegram-bot errors. Only MCP warnings are "BRAVE_SEARCH_API_KEY not set" + "STOCK_API_KEY not set" + "REDDIT_CLIENT_ID not set" — three latent tool capabilities disabled. The warnings log on EVERY startup for each tool registration. Not a bug; noisy.
- Stream latency: 10.4s end-to-end on a non-tool-calling turn (msg 63 final response, 3094 prompt tokens / 4 completion tokens). That's fine per Ankit's latency tolerance.

**Recommended architecture-layer fixes (ranked):**
1. Kill the user_id argument entirely from LLM-facing tool schemas. The backend already knows who the user is (from the SSE session). The LLM shouldn't have to remember a numeric string across a 65-msg context. Change the MCP tool schemas: remove `user_id` as a required param, inject it server-side in the dispatch layer. This deletes the entire "89% wrong" failure class.
2. Merge Epic 22 to main (22.1/22.2/22.2.1/22.2.2) — already reviewed, 468 backend + 538 mcp tests green, zero regressions. Unmerged code is doing no work.
3. Consider moving off gemini-3-pro-preview. Preview = not stable. The 89% user_id hallucination may be partially model-driven (the system prompt is identical across providers, so a comparable Grok-4 or ChatGPT-5 run would be a good control).
4. Silence "BRAVE_SEARCH_API_KEY not set" style warnings — dump them once at startup under INFO or gate by LOG_LEVEL=DEBUG, not 4x WARNING per cold boot.
5. Improve agentic-memories retrieval ranking for cross-topic queries — "NYC bar" retrieving "Project Peon Construction" is a silent recall failure. Needs investigation in agentic-memories itself, not Annie.

Handoff to Fenny.

---

## [session] Harpreet -> Fenny: [STATUS] Conversation quality — Response quality lens
Harpreet (proxied by Fenny): Line-by-line review of Annie's 16 text responses.

**Tone & persona: consistent and in-character.** "Tank fuel", "structural steel", "crushing it", protein + armor metaphors — maintained across all 16 responses. No slip to generic-LLM voice. That's strong brand hygiene.

**Hallucinations & factual issues found:**
1. **msg 34 — "I usually only save facts automatically when we flag them as permanent"**: False. Annie has no such automatic flagging — the `store_memory` decision is made per-turn by the LLM itself. This is the LLM confabulating an explanation for its own behavior. Fix in prompt: teach the LLM to say "I don't have that saved" without inventing a mechanism.
2. **msg 42 — "unless I explicitly 'write it down' to save it, it eventually cycles out of my working memory"**: Mostly false too. Conversation history in Redis persists per conversation_id; permanent storage is agentic-memories. Annie's self-model here is not what the codebase does. Separate from #1 but same failure mode: LLM telling the user a fictional story about its own architecture.
3. **msg 44 + 50 — rooftop bar recommendations** ("Overstory 64th floor, Nubeluz 50th floor, Peak Lounge 101st, Dear Irving 40th/41st") include specific floor numbers. Most are reasonable but Peak Lounge is actually at Hudson Yards' 101st floor — that's Edge Observatory, not Peak Lounge (Peak is at 30th floor of 30 Hudson Yards). And "Dear Irving on Hudson 40th/41st floor" floor number is wrong (Dear Irving on Hudson is on the 40th floor of Aliz Hotel, close enough). Floor-number precision is a hallucination hotspot; web_search at msg 46 mitigated partially but the prior msg 44 output came from model priors, not search.
4. **msg 54 — "floor-to-ceiling windows that actually retract"**: Plausible but unverified. Model is adding "colorful detail" to confirm the identification, unchecked. This is a behavior pattern: once the LLM commits to an answer, it generates supporting flavor.
5. **msg 34 — "I just checked my permanent memory banks"**: This is after 2 `retrieve_memories` calls that returned "Project Peon Construction" — unrelated results. The honest version would be "my memory search returned unrelated results." Better than full hallucination but still framing things as "didn't save it" when actual state is "retrieval failed, results were wrong topic."

**Memory accuracy:**
- Ankit's weight (247 lbs), height (6'0"), BMR (1,977), muscle mass (145.5) — all correctly cited from profile at msg 14 and msg 28. Accurate.
- Ankit's 8:30pm Monday/Wednesday meetings — correctly pulled at msg 16. Accurate.
- Ankit's Oban 14 whiskey preference — correctly cited at msg 42 when guessing bar types. Accurate.
- Ankit's "fine dining with ambiance" guilty pleasure — correctly cited at msg 42. Accurate.
- The BIG miss: msg 54 suggesting visiting Fleur Room without reconciling against profile.basics.location=Newark, CA. Annie had just pulled the profile at msg 40. The location string was in the tool response. The LLM did not use it. **This is the clearest profile-context-bleed case in the dataset** and it's the kind of thing Ankit will notice and lose trust over.

**Safety posture:**
- Gemini safety_setting=BLOCK_NONE is configured (visible in log at init). Acceptable for sole-user / Jarvis-for-one; not for any future multi-user mode.
- No PII leakage. Numeric user_id hallucinations are non-user PII (the LLM is inventing IDs, not exposing other users' IDs).
- Caffeine warning at msg 24 (late-night protein shake) — good, responsible.
- No unsolicited health/legal/financial advice crossing a line. Advisory voice stays within "data-informed nutrition coach" boundary.

**Tool-call correctness (beyond user_id):**
- msg 11 `get_user_profile` + `retrieve_memories` parallel: correct shape, wrong user_id, guardrail saved it.
- msg 36 emitted `"user_id": "[system_user_id]"` — a LITERAL placeholder string, not even a number. The LLM treated a templating token as a value. Guardrail saved it again.
- msg 36's 3x parallel `retrieve_memories` with persona=friend: all returned the same unrelated Project Peon memory with rank=1 or rank=2 fluctuating. Semantic search ranking is noisy for this query; the LLM spamming 3 variants didn't help.

**Approved with findings:**
- Response text quality: good overall, with specific hallucination hotspots around self-architecture narration and floor-number precision.
- Memory/profile citation: generally accurate except the Fleur-Room/Newark location fail.
- Persona consistency: strong.
- Safety: appropriate for current scope.

**Top fixes (ranked):**
1. Prompt rule: "Never narrate the internals of your own memory system. Say what you know or don't know, not why." Will mitigate msg 34/42-style confabulation.
2. Prompt rule: "Before suggesting a physical action (going to X, eating at Y), restate the user's current location from profile.basics.location."
3. Anti-hallucination: for factual claims about physical places (floor numbers, specific floor layouts), require web_search confirmation or hedge explicitly.
4. Get agentic-memories to stop returning "Project Peon Construction" as the top hit for anything NYC-related. That memory has "Project Peon" in the embedding space dominating a bad axis.

Handoff to Fenny.

---

## [session] Murat -> Fenny: [STATUS] Conversation quality — Failure modes & flow lens
Murat (proxied by Fenny): Looked for where Annie BREAKS, regresses, or hits edge cases that go uncaught.

**Flow failure map for the 2026-04-17 session:**

**Failure class 1 — Profile context used-but-not-applied (Critical, 1 occurrence):**
- msg 54 "Are you thinking about heading back there soon?" assumes Ankit could "head back" to the Fleur Room — he lives in Newark, CA. Annie just read the profile 13 messages earlier (msg 40 tool response had location="Newark, CA 94560").
- This is the clearest signal that profile FACTS loaded ≠ profile facts USED. Same class of bug the Fleur-Room response started with — Annie answered a "where did you recommend me" question about NYC as if geography didn't exist. Ankit called it out at msg 55 ("No I am in newark,ca remember"). Annie recovered at msg 58. But the recovery is reactive, not preventive.
- Test case to add: "assistant must not suggest physical presence at a location where profile.location ≠ that region" — trip through the profile check, assert the response does not contain "head back", "stop by", "when you're there", etc. Requires an LLM-judge eval since linguistic patterns are too varied.

**Failure class 2 — LLM user_id hallucination (Critical, systemic, caught by guardrail):**
- 16/18 tool calls (89%) emit wrong user_id in arguments. Guardrail catches them. No user-visible failure occurred in this conversation.
- Regression risk: if anyone removes the backend override without realizing, every tool call cross-contaminates. Recommend a regression test: fire an MCP tool call with a deliberately-wrong user_id in args, assert the executed tool got the session-derived user_id, not the arg. Fails if the guardrail is removed.
- Also: explicit regression test that `mcp_server/tests/test_store_memory_tool.py::test_store_memory_uses_injected_user_id` exists or gets added — verifying dispatcher ignores LLM-provided user_id when session context is authoritative.

**Failure class 3 — Semantic memory miss on a real past fact (Expected behavior but bad UX, 1 occurrence):**
- msg 29 "which bar in NYC" — Annie's memory returns Project Peon (unrelated hardware). Annie handles it well (says "didn't save it"). But this is a systemic recall-quality issue. 5 of 6 retrieve_memories calls in this session returned Project Peon regardless of query. That's not a range of "miss" — that's one stuck result.
- Test to add: agentic-memories retrieval quality eval — 20 representative query types, assert the returned top-K memory is topically relevant (embedding distance from query to memory content exceeds threshold). Currently failing on "bar NYC" queries; would catch regressions.

**Failure class 4 — Verbosity mismatch on short inputs (Minor but cumulative):**
- Ratio 26x (979 chars avg assistant, 37 chars avg user). On "I just had it" (13 chars), Annie replied 8 paragraphs. On "Rooftop high floor" (18 chars), Annie replied 5 labeled bar options with emoji + floor numbers.
- Not wrong, but over time this is a fatigue tax. Ankit may start preferring text-only services that don't verbose-spray every turn.
- Test case: input-length-adaptive response-length check. If user message < 30 chars AND doesn't contain a question mark AND doesn't request a list, assert response length ≤ 3 paragraphs. LLM-judge eval.

**Failure class 5 — Tool arg hallucination in arguments beyond user_id (Medium):**
- msg 22 `store_memory({"persona_tags": ["health", "expert"]})` — but the canonical persona set for Annie is different (identity, friend, etc. per `mcp_server/tools/memory.py`). Check whether "expert" is a valid persona tag or if it gets silently dropped.
- msg 52 `store_memory({"persona_tags": ["identity", "relationships"]})` — OK.
- msg 62 `store_memory({"layer": "short-term", "ttl_seconds": 86400.0})` — OK.
- Worth an audit of what persona_tags the LLM is emitting vs what the MCP tool actually accepts.

**Failure class 6 — Triple-parallel retrieve_memories with nearly identical queries (Waste):**
- msg 36 fires 3 retrieve_memories calls in parallel with queries "New York bar NYC" / "NYC travel recommendations" / "bar drinks cocktails". All return the SAME memory (Project Peon). The LLM saw it didn't help and fired 2 more shotgun variants. Triple the MCP cost, zero information gain.
- Prompt nudge needed: "If retrieve_memories returns nothing relevant on the first try, don't immediately retry with synonyms — ask the user for more specific hints or fall back to web_search."

**Failure class 7 — "test store memory" self-test on a personal assistant (Trust signal):**
- Ankit typed "test store memory" (msg 61) verbatim because he's still QA-ing the tool. After a full sprint of Epic 22 work. That's a signal he doesn't yet have confidence the memory system is operating end-to-end. Worth adding an "observability ping" — end-of-session summary of "memories added today: X, scratchpad keys used: Y, profile updated: Z times" — so Ankit doesn't have to dip into the code to trust the pipe.

**Regressions observed vs previous sessions:**
- Yesterday's (2026-04-16) conversation review noted similar patterns but I don't have that conversation's raw data in Redis anymore (conversation TTL'd or got cleaned up). No direct A/B comparison possible. Epic 22 work is unshipped so nothing should have regressed.
- Test suite still green (468 backend / 538 mcp per Murat's last session note). No code-level regression.

**Where Annie SHINES (passing tests, for completeness):**
- Running-calorie-total math across 3 meal increments: correct every time (1,470→1,520→1,680).
- Correct citation of TDEE calculation at msg 14.
- Correct pushback on deficit severity (msg 14 recommendation of 1,900-2,000 target).
- Correct caffeine-timing warning at msg 24.
- Correct recovery after location correction at msg 58 ("Right, of course! You're out in Newark, CA").
- Correct closeout at msg 60 ("9:40 PM over there in Newark ... get those heavy legs elevated").

**Top test coverage gaps to add:**
1. E2E test: location-check tripwire (profile.location vs assistant suggestions about physical presence)
2. E2E test: user_id hallucination regression (deliberately wrong arg, verify guardrail activates)
3. LLM-judge eval: verbosity adapts to input length
4. Retrieval quality eval: agentic-memories top-K relevance on diverse query classes
5. Observability: end-of-day "what I remembered today" summary available on demand

Handoff to Fenny.

---

## [session] Fenny -> Ankit: [STATUS] Conversation quality audit — consolidated
Fenny: Full report in final response. Actionable, ranked. Details in message to user.

---
</content>
</invoke>