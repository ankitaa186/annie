# Annie Web UI Environment Variables
# Copy this file to .env and configure as needed

# API URL - In development, Vite proxy handles /api routing
# In production, this is configured via Docker build args
VITE_API_URL=/api
