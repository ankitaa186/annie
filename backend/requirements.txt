# Backend API Requirements
# Python 3.12+ required

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# HTTP client for MCP server and LLM APIs
httpx>=0.25.0

# SSE support for streaming responses
sse-starlette>=1.6.5

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
freezegun>=1.2.0  # Time mocking for tests

# State management (Story 2.5)
redis>=5.0.0

# Date/time utilities
pytz>=2025.2

# Debugging (dev environment only)
debugpy>=1.8.0

# Observability - LLM tracing (Story 8.1)
langfuse==2.36.0

# Gemini 3 Pro Provider (Story 9.2; migrated to google.genai SDK in Story 24.1)
# Hotfix 2026-04-25: bumped floor to >=1.40. Empirically verified: the
# installed image had 1.2.0, where types.Part has extra='forbid' and no
# thought_signature field, so the SDK silently dropped the signed
# function_call from chat curated history. Next-turn request then sent
# function_response without a signed function_call ahead of it →
# 400 INVALID_ARGUMENT "Function call is missing a thought_signature in
# functionCall parts". 1.40.0 was confirmed to define
# `thought_signature: Optional[bytes]` on Part (verified by inspecting
# the wheel's google/genai/types.py).
google-genai>=1.40

# Proactive AI Worker (Epic 13)
arq>=0.25.0  # Background job queue (Story 13.7)
yfinance>=0.2.0  # Stock price data for condition evaluators (Story 13.4)
croniter>=1.3.0  # Cron expression parsing (Story 13.7)
# Hotfix 2026-04-25: bumped from 20.7 to >=22.4. The google-genai>=1.40
# upgrade (required for thought_signature support, see comment above) pulls
# httpx>=0.28.1, which conflicts with python-telegram-bot's older
# httpx~=0.27 pin. PTB 22.4 was the first release widening the bound to
# httpx<0.29,>=0.27 (verified by inspecting the wheel METADATA). Backend
# uses only telegram.Bot, bot.send_message, bot.send_chat_action, and the
# error types RetryAfter/BadRequest/TelegramError — all of these surface
# names are unchanged in the 20→22 line, but a focused smoke-test of
# proactive delivery (Story 13.9) is recommended after this upgrade.
python-telegram-bot>=22.4  # Telegram delivery (Story 13.9)

# Document text extraction - ChatGPT fallback (Epic 18)
pypdf>=4.0.0           # PDF text extraction
python-docx>=1.1.0     # DOCX text extraction
openpyxl>=3.1.0        # XLSX text extraction

# Cloudflare Access authentication (Epic 20)
pyjwt>=2.8.0           # JWT decoding for CF-Access-JWT-Assertion
