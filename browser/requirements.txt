# Browser Service Requirements
# Python 3.12+ required

# FastAPI for HTTP server
fastapi>=0.104.0
uvicorn>=0.24.0

# Patchright: anti-detect fork of Playwright (drop-in API).
# Pinned to the branch that tracks Playwright 1.50.x to match the base image.
patchright>=1.50.0

# Timezone handling
pytz>=2024.1

# HTTP client (for healthcheck)
httpx>=0.24.0
